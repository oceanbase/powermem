# Copyright (c) 2026 OceanBase.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

openapi: 3.0.3
info:
  title: PowerContext API
  description: Remote PowerContext transport. Runtime behavior is reported by /v1/capabilities.
  version: 0.2.0
security:
  - BearerAuth: []
  - {}
paths:
  /health/live:
    get:
      security: []
      tags: [health]
      summary: Get process liveness
      operationId: get_liveness
      responses:
        "200":
          description: The API process is alive.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
  /health/ready:
    get:
      security: []
      tags: [health]
      summary: Get deployment readiness
      operationId: get_readiness
      responses:
        "200":
          description: Required Server bindings are ready; optional capabilities may be degraded.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessResponse"
        "503":
          description: Required Server bindings are not ready.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessResponse"
  /v1/capabilities:
    get:
      tags: [capabilities]
      summary: Get runtime capabilities
      operationId: get_capabilities
      x-powercontext-access: {action: server.observe, resource: {type: server}}
      responses:
        "200":
          description: Behavior enabled by the assembled runtime.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Capabilities"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/scopes:
    get:
      tags: [scopes]
      summary: List observable Scopes
      operationId: list_scopes
      x-powercontext-access: {action: server.observe, resource: {type: server}}
      responses:
        "200":
          description: Durable Scope metadata in deterministic identity order.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScopePage"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
    post:
      tags: [scopes]
      summary: Create an independent Scope boundary
      operationId: create_scope
      x-powercontext-access: {action: server.admin, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateScopeRequest"
      responses:
        "201":
          description: The durable Scope descriptor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScopeDescriptor"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "422":
          $ref: "#/components/responses/InvalidRequest"
  /v1/artifact-publications:
    post:
      tags: [scopes]
      summary: Publish one exact Artifact revision into another Scope
      description: >-
        Memory publication is rejected until its complete family-owned state can be created atomically in the target Scope.
      operationId: publish_artifact
      x-powercontext-access: {resolver: publish_artifact_access}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublishArtifactRequest"
      responses:
        "201":
          description: Independent target Artifact and its exact source provenance.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactPublication"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "422":
          $ref: "#/components/responses/InvalidRequest"
  /v1/scopes/{scope_id}:
    get:
      tags: [scopes]
      summary: Get one Scope descriptor
      operationId: get_scope
      x-powercontext-access: {resolver: path_scope_read_access}
      parameters:
        - name: scope_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '.*\S.*'
      responses:
        "200":
          description: The exact Scope descriptor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScopeDescriptor"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
    put:
      tags: [scopes]
      summary: Replace mutable Scope metadata and relationships
      operationId: update_scope
      x-powercontext-access: {resolver: path_scope_admin_access}
      parameters:
        - name: scope_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '.*\S.*'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateScopeRequest"
      responses:
        "200":
          description: The updated Scope descriptor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScopeDescriptor"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "422":
          $ref: "#/components/responses/InvalidRequest"
  /v1/scopes/default:
    get:
      tags: [scopes]
      summary: Get the default Scope binding target
      operationId: get_default_scope
      x-powercontext-access: {action: server.observe, resource: {type: server}}
      responses:
        "200":
          description: The ordinary Scope selected by the host default pointer.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScopeDescriptor"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
    put:
      tags: [scopes]
      summary: Change the default Scope binding target
      operationId: set_default_scope
      x-powercontext-access: {action: server.admin, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetDefaultScopeRequest"
      responses:
        "200":
          description: The selected ordinary Scope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScopeDescriptor"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/scopes/selection/resolve:
    post:
      tags: [scopes]
      summary: Resolve an observation selection to a frozen Scope set
      operationId: resolve_scope_selection
      x-powercontext-access: {resolver: scope_selection_read_access}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResolveScopeSelectionRequest"
      responses:
        "200":
          description: The selected Scope descriptors in deterministic order.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScopePage"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "422":
          $ref: "#/components/responses/InvalidRequest"
  /v1/scope-bindings/resolve:
    post:
      tags: [scope-bindings]
      summary: Resolve an explicit durable or default Scope binding
      operationId: resolve_scope_binding
      x-powercontext-access: {action: server.observe, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResolveScopeBindingRequest"
      responses:
        "200":
          description: The resolved Scope descriptor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScopeDescriptor"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "422":
          $ref: "#/components/responses/InvalidRequest"
  /v1/scope-bindings:
    put:
      tags: [scope-bindings]
      summary: Persist an external identity to Scope binding
      operationId: set_scope_binding
      x-powercontext-access: {action: server.admin, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetScopeBindingRequest"
      responses:
        "200":
          description: The durable external binding.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScopeBinding"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "422":
          $ref: "#/components/responses/InvalidRequest"
  /v1/scope-bindings/clear:
    post:
      tags: [scope-bindings]
      summary: Remove one durable external Scope binding
      operationId: clear_scope_binding
      x-powercontext-access: {action: server.admin, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClearScopeBindingRequest"
      responses:
        "200":
          description: Whether a durable binding was removed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClearScopeBindingResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "422":
          $ref: "#/components/responses/InvalidRequest"
  /v1/sources/content:
    post:
      tags: [sources]
      summary: Capture durable ContentSource evidence
      description: Accept raw content as an idempotent Source without synchronously deriving Artifacts.
      operationId: capture_content_source
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CaptureContentSourceRequest"
      responses:
        "202":
          description: The Source is durably stored for later processing.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CaptureContentSourceResponse"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/source-definitions/register:
    post:
      tags: [source-ingestion]
      summary: Register a worker-owned Source Definition manifest
      description: Registers an immutable declarative manifest without loading worker plugin code.
      operationId: register_source_definition
      x-powercontext-access: {action: server.admin, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterSourceDefinitionRequest"
      responses:
        "200":
          description: The exact manifest is registered or was already registered identically.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SourceDefinitionManifest"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "422":
          $ref: "#/components/responses/InvalidRequest"
  /v1/connector-checkpoints/get:
    post:
      tags: [source-ingestion]
      summary: Read a Connector binding checkpoint
      operationId: get_connector_checkpoint
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: binding.scope_id}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetConnectorCheckpointRequest"
      responses:
        "200":
          description: The current opaque checkpoint, including a normal null initial value.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectorCheckpointState"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
  /v1/source-observations:
    post:
      tags: [source-ingestion]
      summary: Submit a worker-materialized Source observation
      description: Validates the observation against its registered manifest and durably appends it before receipt.
      operationId: submit_source_observation
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: scope_id}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitSourceObservationRequest"
      responses:
        "202":
          description: The observation is durably accepted and can be referenced exactly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SourceObservationReceipt"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "409":
          $ref: "#/components/responses/Conflict"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/InvalidRequest"
  /v1/connector-checkpoints/commit:
    post:
      tags: [source-ingestion]
      summary: Commit a Connector binding checkpoint
      description: Replaces the checkpoint only when its expected starting value still matches.
      operationId: commit_connector_checkpoint
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: binding.scope_id}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CommitConnectorCheckpointRequest"
      responses:
        "200":
          description: The new opaque checkpoint is durable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectorCheckpointState"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
  /v1/context/prepare:
    post:
      tags: [context]
      summary: Prepare bounded context for an Agent turn
      description: Prepare final, ephemeral context from Runtime-owned sources without persisting or injecting it.
      operationId: prepare_context
      x-powercontext-access: {action: scope.read, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrepareContextRequest"
      responses:
        "200":
          description: Final context ready for direct injection, or a normal empty result.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreparedContext"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/work/contracts/create:
    post:
      tags: [work]
      summary: Create a grounded Work Contract
      description: Persist an inspectable delegation baseline without granting execution authority.
      operationId: create_work_contract
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkContractRequest"
      responses:
        "202":
          description: The Work Contract is durably captured as exact Source evidence.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkSourceReceipt"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/work/handoffs/prepare-current:
    post:
      tags: [work]
      summary: Hand off current work in one high-level operation
      description: Capture an inspected boundary and prepare a temporary evidence-bearing Handoff without committing it.
      operationId: handoff_current_work
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HandoffCurrentWorkRequest"
      responses:
        "200":
          description: The captured boundary and Prepared Handoff ready for explicit transfer.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreparedWorkHandoff"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/work/handoffs/acknowledge:
    post:
      tags: [work]
      summary: Resolve and acknowledge a Handoff
      description: Re-resolve one prepared or exact Handoff, check evidence, and capture the receiver's explicit live-state, capability, and authorization checks.
      operationId: acknowledge_handoff
      x-powercontext-access: {resolver: acknowledge_handoff_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcknowledgeHandoffRequest"
      responses:
        "200":
          description: The resolved Handoff and durable receiver acknowledgement.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HandoffAcknowledgement"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/work/outcomes/record:
    post:
      tags: [work]
      summary: Record a completion-aware Task Outcome
      description: Preserve one attempt's status and checks, optionally linked to the exact accepted Handoff Receipt that the result covers.
      operationId: record_task_outcome
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordTaskOutcomeRequest"
      responses:
        "202":
          description: The Task Outcome is durably captured for Handoff evidence and reviewed Experience incubation.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkSourceReceipt"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/handoff/activate:
    post:
      tags: [handoff]
      summary: Activate Handoff generation at a Source boundary
      description: Evaluate the standard Handoff Trigger and synchronously execute any emitted PrepareHandoff Action.
      operationId: activate_handoff
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ActivateHandoffRequest"
      responses:
        "200":
          description: A generated inspectable Draft, or an ignored boundary that was already consumed.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HandoffActivation"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/handoff/prepare:
    post:
      tags: [handoff]
      summary: Generate an inspectable Handoff Draft
      operationId: prepare_handoff
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrepareHandoffRequest"
      responses:
        "200":
          description: An uncommitted Draft generated from the selected exact evidence.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HandoffDraft"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/handoff/finalize:
    post:
      tags: [handoff]
      summary: Finalize an inspected Handoff Draft
      operationId: finalize_handoff
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinalizeHandoffRequest"
      responses:
        "200":
          description: A temporary Handoff ready for direct transfer or explicit commit.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreparedHandoff"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/handoff/commit:
    post:
      tags: [handoff]
      summary: Commit an explicit Handoff milestone
      operationId: commit_handoff
      x-powercontext-access: {resolver: commit_handoff_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CommitHandoffRequest"
      responses:
        "200":
          description: The committed immutable Handoff Revision.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommittedHandoff"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/handoff/continue:
    post:
      tags: [handoff]
      summary: Resolve a Handoff as untrusted historical input
      operationId: continue_handoff
      x-powercontext-access: {resolver: continue_handoff_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContinueHandoffRequest"
      responses:
        "200":
          description: Resolved content and per-statement evidence availability.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HandoffResolution"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/memory/flush:
    post:
      tags: [memory]
      summary: Process the pending Source window into Memory
      description: Run one bounded Source-to-Memory activation for operational control and testing.
      operationId: flush_memory
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FlushMemoryRequest"
      responses:
        "200":
          description: The activation completed or found no pending Sources.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FlushMemoryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/memory/remember:
    post:
      tags: [memory]
      summary: Remember explicit Memory content
      description: Save one already-curated Memory entry without creating a Source or invoking extraction.
      operationId: remember_memory
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RememberMemoryRequest"
      responses:
        "200":
          description: The explicit Memory mutation completed.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryMutationResponse"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/memory/search:
    post:
      tags: [memory]
      summary: Search active Memory entries
      description: Retrieve relevant active Memory entries within one explicit application scope.
      operationId: search_memory
      x-powercontext-access: {action: scope.read, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SearchMemoryRequest"
      responses:
        "200":
          description: Matching Memory entries, or an empty result when the scope has no Memory.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SearchMemoryResponse"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/memory/entries/list:
    post:
      tags: [memory]
      summary: List Memory entries
      description: >-
        Read active entries from the current Memory head. Inactive entries are available only when explicitly
        requested for audit.
      operationId: list_memory_entries
      x-powercontext-access: {action: scope.read, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ListMemoryEntriesRequest"
      responses:
        "200":
          description: The selected entries from the current Memory head.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListMemoryEntriesResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/memory/entries/get:
    post:
      tags: [memory]
      summary: Get an exact Memory entry version
      description: Resolve an immutable entry citation within one Memory Revision.
      operationId: get_memory_entry
      x-powercontext-access: {resolver: exact_memory_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetMemoryEntryRequest"
      responses:
        "200":
          description: The exact Memory entry version.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryEntry"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/memory/entries/revise:
    post:
      tags: [memory]
      summary: Revise an exact Memory entry
      description: Replace active entry content against an explicit current Memory Revision.
      operationId: revise_memory_entry
      x-powercontext-access: {resolver: exact_memory_write_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviseMemoryEntryRequest"
      responses:
        "200":
          description: The Memory entry revision completed.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryMutationResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/memory/entries/retire:
    post:
      tags: [memory]
      summary: Retire an exact Memory entry
      description: Deactivate an entry against an explicit current Memory Revision without deleting history.
      operationId: retire_memory_entry
      x-powercontext-access: {resolver: exact_memory_write_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RetireMemoryEntryRequest"
      responses:
        "200":
          description: The Memory entry retirement completed.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryMutationResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/memory/changes:
    post:
      tags: [memory]
      summary: List Memory Revision changes
      description: Read compact entry changes without expanding entry bodies.
      operationId: list_memory_changes
      x-powercontext-access: {action: scope.read, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ListMemoryChangesRequest"
      responses:
        "200":
          description: Compact changes through the selected Memory Revision.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListMemoryChangesResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/experience/propose:
    post:
      tags: [experience]
      summary: Propose Experience content
      description: Persist a pending Experience Candidate without creating an Artifact Revision.
      operationId: propose_experience
      x-powercontext-access: {resolver: experience_candidate_write_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProposeExperienceRequest"
      responses:
        "201":
          description: The pending Experience Candidate.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactCandidate"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/experience/generate:
    post:
      tags: [experience]
      summary: Generate an Experience Candidate
      description: Use the configured model and caller-selected exact evidence; persist only a schema-valid pending Candidate.
      operationId: generate_experience
      x-powercontext-access: {resolver: experience_candidate_write_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateExperienceRequest"
      responses:
        "200":
          description: A pending Candidate or an explicit semantic no-op.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratedCandidateResponse"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/experience/get:
    post:
      tags: [experience]
      summary: Get an exact Experience Revision
      description: Read approved Experience content and its exact direct evidence.
      operationId: get_experience
      x-powercontext-access: {resolver: exact_experience_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetExperienceRequest"
      responses:
        "200":
          description: The exact approved Experience Revision.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExperienceArtifact"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/propose:
    post:
      tags: [skill]
      summary: Propose managed Skill content
      description: Persist a pending managed Skill Candidate without creating an Artifact Revision.
      operationId: propose_skill
      x-powercontext-access: {resolver: skill_candidate_write_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProposeSkillRequest"
      responses:
        "201":
          description: The pending managed Skill Candidate.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactCandidate"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/generate:
    post:
      tags: [skill]
      summary: Generate a managed Skill Candidate
      description: Use the configured model with an explicit provenance shape; persist only a schema-valid pending Candidate.
      operationId: generate_skill
      x-powercontext-access: {resolver: skill_candidate_write_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateSkillRequest"
      responses:
        "200":
          description: A pending Candidate or an explicit semantic no-op.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratedCandidateResponse"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/get:
    post:
      tags: [skill]
      summary: Get an exact managed Skill Revision
      description: Read approved managed Skill content and its exact direct evidence.
      operationId: get_skill
      x-powercontext-access: {resolver: exact_skill_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetSkillRequest"
      responses:
        "200":
          description: The exact approved managed Skill Revision.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillArtifact"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/library:
    post:
      tags: [skill]
      summary: List or search current managed Skills
      description: Return current managed Skill heads with lifecycle governance; retired Skills remain exact-read only.
      operationId: list_managed_skills
      x-powercontext-access: {action: scope.read, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ListManagedSkillsRequest"
      responses:
        "200":
          description: Current managed Skill Library rows.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListManagedSkillsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/lifecycle:
    post:
      tags: [skill]
      summary: Update managed Skill lifecycle
      description: Apply an explicit lifecycle transition using governance generation CAS without changing package bytes.
      operationId: update_skill_lifecycle
      x-powercontext-access: {resolver: skill_identity_write_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSkillLifecycleRequest"
      responses:
        "200":
          description: Updated managed Skill governance.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillGovernance"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/package/manifest:
    post:
      tags: [skill]
      summary: Get an exact managed Skill package manifest
      description: Return verified metadata and file inventory without executing or returning file bodies.
      operationId: get_skill_package_manifest
      x-powercontext-access: {resolver: exact_skill_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetSkillPackageRequest"
      responses:
        "200":
          description: Verified exact package manifest.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillPackageManifest"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/package/download:
    post:
      tags: [skill]
      summary: Download an exact managed Skill package
      description: Return canonical ZIP bytes as bounded base64 with their content-addressed reference.
      operationId: download_skill_package
      x-powercontext-access: {resolver: exact_skill_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetSkillPackageRequest"
      responses:
        "200":
          description: Canonical exact package archive.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillPackageDownload"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/package/propose:
    post:
      tags: [skill]
      summary: Propose an uploaded standard Skill package
      description: Canonicalize exact ZIP bytes, store them once, and create a pending Candidate without LLM rewriting.
      operationId: propose_skill_package
      x-powercontext-access: {resolver: skill_candidate_write_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProposeSkillPackageRequest"
      responses:
        "201":
          description: Pending exact package Candidate.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactCandidate"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/usage:
    post:
      tags: [skill]
      summary: Record a bounded Skill usage observation
      description: Validate an exact managed Skill Revision and capture immutable bounded usage Source evidence.
      operationId: record_skill_usage
      x-powercontext-access: {resolver: skill_usage_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordSkillUsageRequest"
      responses:
        "201":
          description: Accepted immutable usage Source evidence.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CaptureContentSourceResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/remote/targets:
    post:
      tags: [skill]
      summary: List remote Agent Skill target status
      description: Return credential-free target metadata and desired/observed publication state for one scope.
      operationId: list_remote_skill_targets
      x-powercontext-access: {action: scope.admin, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ListRemoteSkillTargetsRequest"
      responses:
        "200":
          description: Remote target status rows visible to the administrative caller.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListRemoteSkillTargetsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/remote/target/create:
    post:
      tags: [skill]
      summary: Create a remote Agent Skill target enrollment
      description: Create a pending project target and return one short-lived enrollment code exactly once.
      operationId: create_remote_skill_target
      x-powercontext-access: {action: scope.admin, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRemoteSkillTargetRequest"
      responses:
        "201":
          description: Pending remote target enrollment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RemoteSkillTargetEnrollment"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/remote/target/enroll:
    post:
      security: []
      tags: [skill]
      summary: Enroll a remote Agent Skill Receiver
      description: Consume one short-lived enrollment code and return a per-target credential exactly once.
      operationId: enroll_remote_skill_target
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EnrollRemoteSkillTargetRequest"
      responses:
        "200":
          description: Activated remote target credential.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RemoteSkillTargetCredential"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/remote/target/rename:
    post:
      tags: [skill]
      summary: Rename a remote Agent Skill target
      description: Change the human-readable target name with target generation CAS while retaining its durable identity.
      operationId: rename_remote_skill_target
      x-powercontext-access: {action: scope.admin, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RenameRemoteSkillTargetRequest"
      responses:
        "200":
          description: Renamed remote target.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RemoteSkillTarget"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/remote/target/revoke:
    post:
      tags: [skill]
      summary: Revoke a remote Agent Skill target
      description: Revoke the per-target credential with target generation CAS while retaining durable identity.
      operationId: revoke_remote_skill_target
      x-powercontext-access: {action: scope.admin, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RevokeRemoteSkillTargetRequest"
      responses:
        "200":
          description: Revoked remote target.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RemoteSkillTarget"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/remote/publication/publish:
    post:
      tags: [skill]
      summary: Set a remote target Skill desired Revision
      description: Advance only Server-owned desired state; delivery is confirmed later by an exact Receipt.
      operationId: publish_remote_skill
      x-powercontext-access: {resolver: publish_remote_skill_access}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublishRemoteSkillRequest"
      responses:
        "200":
          description: Latest remote publication desired state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RemoteSkillPublication"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/remote/publication/unpublish:
    post:
      tags: [skill]
      summary: Set remote target Skill desired absence
      description: Advance desired state without claiming that any remote directory has already been removed.
      operationId: unpublish_remote_skill
      x-powercontext-access: {action: scope.admin, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnpublishRemoteSkillRequest"
      responses:
        "200":
          description: Latest remote publication desired state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RemoteSkillPublication"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/remote/reconcile:
    post:
      security:
        - TargetBearerAuth: []
      tags: [skill]
      summary: Reconcile a remote Agent Skill target
      description: Authenticate one target and return only latest-generation idempotent install or unpublish actions.
      operationId: reconcile_remote_skills
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReconcileRemoteSkillsRequest"
      responses:
        "200":
          description: Latest desired-state actions for this target only.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReconcileRemoteSkillsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/remote/package/download:
    post:
      security:
        - TargetBearerAuth: []
      tags: [skill]
      summary: Download the exact package desired by a remote target
      description: Return canonical ZIP bytes only when target, generation, Artifact Revision, and package reference all match.
      operationId: download_remote_skill_package
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DownloadRemoteSkillPackageRequest"
      responses:
        "200":
          description: Canonical exact package archive.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillPackageDownload"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/skill/remote/receipt:
    post:
      security:
        - TargetBearerAuth: []
      tags: [skill]
      summary: Record an exact remote Skill delivery Receipt
      description: Update latest observed state only after credential, generation, Artifact, operation, and digest validation.
      operationId: record_remote_skill_receipt
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordRemoteSkillReceiptRequest"
      responses:
        "200":
          description: Receipt acceptance and latest publication observation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RemoteSkillReceiptResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/external-skills/scan:
    post:
      tags: [skill]
      summary: Scan configured external Skill roots
      description: Replace the current host-local Registry projection without copying or rewriting package content.
      operationId: scan_external_skills
      x-powercontext-access: {action: server.admin, resource: {type: server}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScanExternalSkillsRequest"
      responses:
        "200":
          description: The rebuildable provider snapshot.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScanExternalSkillsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/external-skills/list:
    post:
      tags: [skill]
      summary: List external Skills visible on this host
      description: Return live local resolutions; unavailable registrations are omitted unless explicitly requested.
      operationId: list_external_skills
      x-powercontext-access: {action: server.observe, resource: {type: server}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ListExternalSkillsRequest"
      responses:
        "200":
          description: External Skills resolved against the current Agent, host, scope, and fingerprint.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListExternalSkillsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/external-skills/resolve:
    post:
      tags: [skill]
      summary: Resolve an exact external Skill fingerprint
      description: Resolve only the registered local package version requested by the caller; never install or fall back.
      operationId: resolve_external_skill
      x-powercontext-access: {action: server.observe, resource: {type: server}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResolveExternalSkillRequest"
      responses:
        "200":
          description: The live exact-resolution result, which may be unavailable.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExternalSkillResolution"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/external-skills/import:
    post:
      tags: [skill]
      summary: Import or fork an external Skill into Review
      description: Capture one exact local snapshot and use the configured model to propose a new managed Skill Candidate.
      operationId: import_external_skill
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImportExternalSkillRequest"
      responses:
        "200":
          description: A pending managed Skill Candidate or an explicit semantic no-op.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratedCandidateResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/artifact-candidates/list:
    post:
      tags: [review]
      summary: List Artifact Candidates
      description: Page current Candidate heads; pending is the default Review Inbox view.
      operationId: list_artifact_candidates
      x-powercontext-access: {action: scope.read, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ListArtifactCandidatesRequest"
      responses:
        "200":
          description: The selected current Candidate heads.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactCandidatePage"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/artifact-candidates/get:
    post:
      tags: [review]
      summary: Get an Artifact Candidate
      description: Read the current head and exact immutable proposal version.
      operationId: get_artifact_candidate
      x-powercontext-access: {action: scope.read, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetArtifactCandidateRequest"
      responses:
        "200":
          description: The current Candidate head.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactCandidate"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/artifact-candidates/approve:
    post:
      tags: [review]
      summary: Approve an Artifact Candidate
      description: Commit the reviewed proposal and mark the Candidate approved in one transaction.
      operationId: approve_artifact_candidate
      x-powercontext-access: {action: scope.review, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveArtifactCandidateRequest"
      responses:
        "200":
          description: The approved Candidate and exact result Artifact.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactCandidate"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/artifact-candidates/reject:
    post:
      tags: [review]
      summary: Reject an Artifact Candidate
      description: Move the exact pending version to its rejected terminal state without writing an Artifact.
      operationId: reject_artifact_candidate
      x-powercontext-access: {action: scope.review, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectArtifactCandidateRequest"
      responses:
        "200":
          description: The rejected Candidate.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactCandidate"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/artifact-candidates/revise:
    post:
      tags: [review]
      summary: Revise an Artifact Candidate
      description: Append a complete replacement proposal as the next immutable pending version.
      operationId: revise_artifact_candidate
      x-powercontext-access: {action: scope.review, resource: {type: scope, scope-id-from: scope_id}}
      x-powercontext-scope-mode: current
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviseArtifactCandidateRequest"
      responses:
        "200":
          description: The next pending Candidate version.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactCandidate"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/stats:
    post:
      tags: [stats]
      summary: Aggregate product statistics over a Scope selection
      operationId: get_stats
      x-powercontext-access: {resolver: scope_selection_read_access}
      x-powercontext-scope-mode: selection
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetStatsRequest"
      responses:
        "200":
          description: Current inventory, model usage, and recall token estimates for the frozen Scope set.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
            Cache-Control:
              description: Prevent caches from retaining scoped statistics.
              schema:
                type: string
                enum: [no-store]
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScopedStats"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/handoff-reports/get:
    post:
      tags: [handoff-reports]
      summary: Generate a Handoff Report
      operationId: get_handoff_report
      x-powercontext-access: {resolver: scope_selection_read_access}
      x-powercontext-scope-mode: selection
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetHandoffReportRequest"
      responses:
        "200":
          description: A canonical JSON report, optionally accompanied by Markdown.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
            Cache-Control:
              description: Prevent caches from retaining scoped report data.
              schema:
                type: string
                enum: [no-store]
            X-PowerContext-Selection-Digest:
              description: Digest of the exact report selection.
              schema:
                type: string
                pattern: '^sha256:[0-9a-f]{64}$'
            X-PowerContext-Report-Digest:
              description: Digest of the selected output projection.
              schema:
                type: string
                pattern: '^sha256:[0-9a-f]{64}$'
            Content-Disposition:
              description: Safe attachment filename when download is true.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HandoffReportResponse"
            text/markdown:
              schema:
                type: string
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "413":
          $ref: "#/components/responses/ReportTooLarge"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/scopes/{scope_id}/sources:
    post:
      tags: [sources]
      summary: Create a durable Source
      description: Persist one Source without synchronously deriving Artifacts. The Server generates source_id.
      operationId: create_source
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: scope_id}}
      parameters:
        - name: scope_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '.*\S.*'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSourceRequest"
      responses:
        "201":
          description: The Source was durably created.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SourceRecord"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/scopes/{scope_id}/sources/{source_type}/{source_id}:
    get:
      tags: [sources]
      summary: Get one exact Source
      operationId: get_source
      x-powercontext-access: {resolver: path_scope_read_access}
      parameters:
        - name: scope_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '.*\S.*'
        - name: source_type
          in: path
          required: true
          schema:
            type: string
            enum: [content]
        - name: source_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '^[\x21-\x7E]+$'
      responses:
        "200":
          description: The exact Source.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SourceRecord"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/scopes/{scope_id}/artifacts:
    post:
      tags: [artifacts]
      summary: Create an Artifact
      description: >-
        Dispatch the family-specific creation command through the owning Family writer and atomically create
        revision one, its derived Family state, and its system provenance Source. Handoff is the Scope singleton:
        Create returns 409 when it already exists and callers must use Replace to update it.
      operationId: create_artifact
      x-powercontext-access: {action: scope.contribute, resource: {type: scope, scope-id-from: scope_id}}
      parameters:
        - name: scope_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '.*\S.*'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateArtifactRequest"
      responses:
        "201":
          description: Artifact revision one was committed.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            ETag:
              $ref: "#/components/headers/ArtifactETag"
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactCreated"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/scopes/{scope_id}/artifacts/{family}:
    get:
      tags: [artifacts]
      summary: List current Artifact heads
      description: List current heads for exactly one built-in Artifact family.
      operationId: list_artifacts
      x-powercontext-access: {action: scope.read, resource: {type: scope, scope-id-from: scope_id}}
      parameters:
        - name: scope_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '.*\S.*'
        - name: family
          in: path
          required: true
          schema:
            type: string
            enum: [memory, experience, skill, handoff]
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 4096
      responses:
        "200":
          description: One stable page of current Artifact heads.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "410":
          $ref: "#/components/responses/CursorExpired"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/scopes/{scope_id}/artifacts/{family}/{artifact_id}:
    get:
      tags: [artifacts]
      summary: Get the current Artifact head
      operationId: get_artifact
      x-powercontext-access: {resolver: path_artifact_read_access}
      parameters:
        - name: scope_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '.*\S.*'
        - name: family
          in: path
          required: true
          schema:
            type: string
            enum: [memory, experience, skill, handoff]
        - name: artifact_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
            pattern: '^[\x21-\x7E]+$'
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: The current visible Artifact head.
          headers:
            ETag:
              $ref: "#/components/headers/ArtifactETag"
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactRevision"
        "304":
          description: If-None-Match identifies the current Artifact head.
          headers:
            ETag:
              $ref: "#/components/headers/ArtifactETag"
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
    put:
      tags: [artifacts]
      summary: Replace the current Artifact head
      description: Commit a complete next revision when If-Match identifies the current head.
      operationId: replace_artifact
      x-powercontext-access: {resolver: path_artifact_write_access}
      parameters:
        - name: scope_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '.*\S.*'
        - name: family
          in: path
          required: true
          schema:
            type: string
            enum: [memory, experience, skill, handoff]
        - name: artifact_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
            pattern: '^[\x21-\x7E]+$'
        - name: If-Match
          in: header
          required: true
          schema:
            type: string
            minLength: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplaceArtifactRequest"
      responses:
        "200":
          description: The complete replacement was committed as the next revision.
          headers:
            ETag:
              $ref: "#/components/headers/ArtifactETag"
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactRevision"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "412":
          $ref: "#/components/responses/PreconditionFailed"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "428":
          $ref: "#/components/responses/PreconditionRequired"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/scopes/{scope_id}/artifacts/{family}/{artifact_id}/revisions/{revision}:
    get:
      tags: [artifacts]
      summary: Get one exact immutable Artifact revision
      operationId: get_artifact_revision
      x-powercontext-access: {resolver: path_artifact_read_access}
      parameters:
        - name: scope_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '.*\S.*'
        - name: family
          in: path
          required: true
          schema:
            type: string
            enum: [memory, experience, skill, handoff]
        - name: artifact_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
            pattern: '^[\x21-\x7E]+$'
        - name: revision
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        "200":
          description: The exact immutable Artifact revision.
          headers:
            X-PowerContext-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactRevision"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/access/me:
    get:
      tags: [access]
      summary: Get the authenticated Principal and Access capabilities
      operationId: get_access_principal
      x-powercontext-access: {action: access.self, resource: {type: server}}
      responses:
        "200":
          description: The opaque Principal and enforceable deployment Access capabilities.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessMeResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/access/check:
    post:
      tags: [access]
      summary: Check one compound authorization requirement
      operationId: check_access
      x-powercontext-access: {action: access.self, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccessCheckRequest"
      responses:
        "200":
          description: The aggregate decision and ordered low-sensitivity requirement decisions.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessCheckResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/access/resources/list:
    post:
      tags: [access]
      summary: List only resources already visible to the Principal
      operationId: list_access_resources
      x-powercontext-access: {action: access.self, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ListAccessResourcesRequest"
      responses:
        "200":
          description: A non-discovering page derived from authorized relationships.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessResourcePage"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/access/roles/list:
    post:
      tags: [access]
      summary: List stable built-in role definitions
      operationId: list_access_roles
      x-powercontext-access: {action: access.self, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ListAccessRolesRequest"
      responses:
        "200":
          description: Stable role names and the resource type accepted by each role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessRolePage"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/access/bindings/list:
    post:
      tags: [access]
      summary: List Access Bindings under an administrative boundary
      operationId: list_access_bindings
      x-powercontext-access: {action: access.self, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ListAccessBindingsRequest"
      responses:
        "200":
          description: Matching immutable Access Bindings.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessBindingPage"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/access/bindings/create:
    post:
      tags: [access]
      summary: Create an idempotent Access Binding
      operationId: create_access_binding
      x-powercontext-access: {action: access.self, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAccessBindingRequest"
      responses:
        "201":
          description: The Access Binding was created or an identical idempotent result was returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessBinding"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/access/bindings/revoke:
    post:
      tags: [access]
      summary: Revoke an Access Binding using compare-and-swap
      operationId: revoke_access_binding
      x-powercontext-access: {action: access.self, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RevokeAccessBindingRequest"
      responses:
        "200":
          description: The revoked Access Binding with its incremented version.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessBinding"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/access/bindings/replace:
    post:
      tags: [access]
      summary: Atomically replace an immutable Access Binding
      operationId: replace_access_binding
      x-powercontext-access: {action: access.self, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplaceAccessBindingRequest"
      responses:
        "200":
          description: The revoked previous Binding and active replacement with the same resource and role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessBindingReplacement"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/access/audit/list:
    post:
      tags: [access]
      summary: List data-minimized Access audit events
      operationId: list_access_audit
      x-powercontext-access: {action: access.self, resource: {type: server}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ListAccessAuditRequest"
      responses:
        "200":
          description: Ordered authorization and relationship audit events.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessAuditPage"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/InvalidRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      description: Bearer credential resolved to an opaque authenticated Principal by the Server deployment.
    TargetBearerAuth:
      type: http
      scheme: bearer
      description: Per-target credential issued once during remote Receiver enrollment.
  headers:
    ArtifactETag:
      description: Opaque strong validator for the current Artifact head. Clients must replay it verbatim.
      schema:
        type: string
        minLength: 1
    BearerChallenge:
      description: Authentication scheme required by the Server.
      schema:
        type: string
        example: Bearer
    RequestId:
      description: Opaque identifier for correlating one request.
      schema:
        type: string
    Location:
      description: URI of the newly created Source or Artifact head.
      schema:
        type: string
  responses:
    BadRequest:
      description: The request query or pagination cursor is invalid.
      headers:
        X-PowerContext-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    CursorExpired:
      description: The pagination cursor has expired.
      headers:
        X-PowerContext-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: A valid bearer token is required by this Server deployment.
      headers:
        WWW-Authenticate:
          $ref: "#/components/headers/BearerChallenge"
        X-PowerContext-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Conflict:
      description: The command conflicts with current immutable state.
      headers:
        X-PowerContext-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    PreconditionFailed:
      description: If-Match does not identify the current Artifact head.
      headers:
        X-PowerContext-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    PreconditionRequired:
      description: A current Artifact ETag is required in If-Match.
      headers:
        X-PowerContext-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InvalidRequest:
      description: The request violates the transport or application contract.
      headers:
        X-PowerContext-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    ReportTooLarge:
      description: The selected Handoff Report exceeds the deterministic output limit.
      headers:
        X-PowerContext-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: The requested durable value was not found or is not observable.
      headers:
        X-PowerContext-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unavailable:
      description: A required Runtime binding or dependency is unavailable.
      headers:
        X-PowerContext-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InternalError:
      description: The Server failed without exposing internal details.
      headers:
        X-PowerContext-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Forbidden:
      description: The authenticated Principal is not authorized for the requested action and resource.
      headers:
        X-PowerContext-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
  schemas:
    ActivateHandoffRequest:
      type: object
      additionalProperties: false
      required: [scope_id, boundary_source, objective]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        boundary_source:
          $ref: "#/components/schemas/SourceReference"
        objective:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
        evidence:
          type: array
          maxItems: 32
          items:
            $ref: "#/components/schemas/HandoffCitation"
          default: []
        max_bytes:
          type: integer
          minimum: 512
          maximum: 32768
          default: 8000
    ArtifactCollectionItem:
      type: object
      required: [scope_id, family, artifact_id, revision, sources, artifacts, content_digest]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        family:
          $ref: "#/components/schemas/BaseArtifactFamily"
        artifact_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        revision:
          type: integer
          minimum: 1
        sources:
          type: array
          items:
            $ref: "#/components/schemas/SourceTypeReference"
        artifacts:
          type: array
          items:
            $ref: "#/components/schemas/ArtifactReference"
        content_digest:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
    ArtifactCreated:
      type: object
      required: [scope_id, family, artifact_id, revision, sources, artifacts]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        family:
          $ref: "#/components/schemas/BaseArtifactFamily"
        artifact_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        revision:
          type: integer
          minimum: 1
        sources:
          type: array
          items:
            $ref: "#/components/schemas/SourceTypeReference"
        artifacts:
          type: array
          items:
            $ref: "#/components/schemas/ArtifactReference"
    ArtifactPage:
      type: object
      required: [items, next_cursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ArtifactCollectionItem"
        next_cursor:
          type: string
          nullable: true
    ArtifactRevision:
      type: object
      required: [scope_id, family, artifact_id, revision, content, sources, artifacts, content_digest]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        family:
          $ref: "#/components/schemas/BaseArtifactFamily"
        artifact_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        revision:
          type: integer
          minimum: 1
        content:
          type: object
          additionalProperties: true
        sources:
          type: array
          items:
            $ref: "#/components/schemas/SourceTypeReference"
        artifacts:
          type: array
          items:
            $ref: "#/components/schemas/ArtifactReference"
        content_digest:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
    ArtifactReference:
      type: object
      additionalProperties: false
      required: [family, artifact_id, revision]
      properties:
        family:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        artifact_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        revision:
          type: integer
          minimum: 1
    ArtifactAddress:
      type: object
      additionalProperties: false
      required: [scope_id, artifact]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        artifact:
          $ref: "#/components/schemas/ArtifactReference"
    PublishArtifactRequest:
      type: object
      additionalProperties: false
      required: [source, target_scope_id, idempotency_key]
      properties:
        source:
          $ref: "#/components/schemas/ArtifactAddress"
        target_scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        idempotency_key:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
    ArtifactPublication:
      type: object
      additionalProperties: false
      required: [source, target, content_digest]
      properties:
        source:
          $ref: "#/components/schemas/ArtifactAddress"
        target:
          $ref: "#/components/schemas/ArtifactAddress"
        content_digest:
          type: string
          pattern: '^[0-9a-f]{64}$'
    ScopeExternalReference:
      type: object
      additionalProperties: false
      required: [kind, value]
      properties:
        kind:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
        value:
          type: string
          minLength: 1
          maxLength: 2000
          pattern: '.*\S.*'
    ScopeDescriptor:
      type: object
      additionalProperties: false
      required: [scope_id, title, summary, context_references, external_references, version]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        title:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        summary:
          type: string
          minLength: 1
          maxLength: 2000
          pattern: '.*\S.*'
        parent_scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
          nullable: true
        context_references:
          type: array
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '.*\S.*'
        external_references:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/ScopeExternalReference"
        version:
          type: integer
          minimum: 1
    ScopePage:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ScopeDescriptor"
    CreateScopeRequest:
      type: object
      additionalProperties: false
      required: [title, summary, idempotency_key]
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        summary:
          type: string
          minLength: 1
          maxLength: 2000
          pattern: '.*\S.*'
        parent_scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
          nullable: true
        context_references:
          type: array
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '.*\S.*'
          default: []
        external_references:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/ScopeExternalReference"
          default: []
        idempotency_key:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
    UpdateScopeRequest:
      type: object
      additionalProperties: false
      required: [expected_version, title, summary]
      properties:
        expected_version:
          type: integer
          minimum: 1
        title:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        summary:
          type: string
          minLength: 1
          maxLength: 2000
          pattern: '.*\S.*'
        parent_scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
          nullable: true
        context_references:
          type: array
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '.*\S.*'
          default: []
        external_references:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/ScopeExternalReference"
          default: []
    SetDefaultScopeRequest:
      type: object
      additionalProperties: false
      required: [scope_id]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
    AllScopeSelection:
      type: object
      additionalProperties: false
      required: [mode]
      properties:
        mode:
          type: string
          enum: [all]
    ExactScopeSelection:
      type: object
      additionalProperties: false
      required: [mode, scope_ids]
      properties:
        mode:
          type: string
          enum: [exact]
        scope_ids:
          type: array
          minItems: 1
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 256
            pattern: '.*\S.*'
    SubtreeScopeSelection:
      type: object
      additionalProperties: false
      required: [mode, root_scope_id]
      properties:
        mode:
          type: string
          enum: [subtree]
        root_scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
    ScopeSelection:
      oneOf:
        - $ref: "#/components/schemas/AllScopeSelection"
        - $ref: "#/components/schemas/ExactScopeSelection"
        - $ref: "#/components/schemas/SubtreeScopeSelection"
      discriminator:
        propertyName: mode
    ResolveScopeSelectionRequest:
      type: object
      additionalProperties: false
      required: [selection]
      properties:
        selection:
          $ref: "#/components/schemas/ScopeSelection"
    ScopeBindingKey:
      type: object
      additionalProperties: false
      required: [integration, kind, external_id]
      properties:
        integration:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
        kind:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '.*\S.*'
        external_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
    ScopeBinding:
      type: object
      additionalProperties: false
      required: [key, scope_id]
      properties:
        key:
          $ref: "#/components/schemas/ScopeBindingKey"
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
    SetScopeBindingRequest:
      $ref: "#/components/schemas/ScopeBinding"
    ClearScopeBindingRequest:
      type: object
      additionalProperties: false
      required: [key]
      properties:
        key:
          $ref: "#/components/schemas/ScopeBindingKey"
    ClearScopeBindingResponse:
      type: object
      additionalProperties: false
      required: [cleared]
      properties:
        cleared:
          type: boolean
    ResolveScopeBindingRequest:
      type: object
      additionalProperties: false
      properties:
        explicit_scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
          nullable: true
        binding_keys:
          type: array
          items:
            $ref: "#/components/schemas/ScopeBindingKey"
          default: []
    CandidatePermissions:
      type: object
      additionalProperties: false
      required: [can_revise, can_approve, can_reject]
      properties:
        can_revise: {type: boolean}
        can_approve: {type: boolean}
        can_reject: {type: boolean}
    HandoffReceiptIdentity:
      type: object
      additionalProperties: false
      required: [principal, receiver_identity_matches]
      description: Server-owned attestation stored separately from the immutable untrusted Receipt.
      properties:
        principal:
          $ref: "#/components/schemas/AccessPrincipal"
        receiver_identity_matches:
          type: boolean
    ArtifactCandidate:
      type: object
      additionalProperties: false
      required:
        - candidate_id
        - version
        - family
        - status
        - proposal
        - source_refs
        - artifact_refs
        - target
        - reason
        - result_artifact
        - decision_reason
      properties:
        permissions:
          $ref: "#/components/schemas/CandidatePermissions"
          nullable: true
          description: Current Principal permissions in enforced mode; advisory and checked again on mutation.
        candidate_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        version:
          type: integer
          minimum: 1
        family:
          $ref: "#/components/schemas/CandidateFamily"
        status:
          $ref: "#/components/schemas/CandidateStatus"
        proposal:
          oneOf:
            - $ref: "#/components/schemas/ExperienceProposal"
            - $ref: "#/components/schemas/SkillProposal"
        source_refs:
          type: array
          maxItems: 32
          description: Exact Source evidence. Counted with artifact_refs toward a combined maximum of 32 references.
          items:
            $ref: "#/components/schemas/SourceReference"
        artifact_refs:
          type: array
          maxItems: 32
          description: Exact Artifact evidence. Counted with source_refs toward a combined maximum of 32 references.
          items:
            $ref: "#/components/schemas/ArtifactReference"
        target:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        reason:
          type: string
          minLength: 1
          maxLength: 2000
          nullable: true
        result_artifact:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        decision_reason:
          type: string
          minLength: 1
          maxLength: 2000
          nullable: true
    ArtifactCandidatePage:
      type: object
      additionalProperties: false
      required: [candidates, next_cursor]
      properties:
        candidates:
          type: array
          items:
            $ref: "#/components/schemas/ArtifactCandidate"
        next_cursor:
          type: string
          nullable: true
    ApproveArtifactCandidateRequest:
      type: object
      additionalProperties: false
      required: [scope_id, candidate_id, expected_version]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        candidate_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        expected_version:
          type: integer
          minimum: 1
    Capabilities:
      type: object
      additionalProperties: false
      required:
        [source_types, artifact_families, memory_extraction, handoff_generation, search_modes, context_versions]
      properties:
        source_types:
          type: array
          items:
            type: string
        artifact_families:
          type: array
          items:
            type: string
        memory_extraction:
          type: boolean
          description: Whether pending Sources can be extracted into Memory.
        experience_generation:
          type: boolean
          default: false
          description: Whether the configured model can generate reviewed Experience Candidates.
        managed_skill_generation:
          type: boolean
          default: false
          description: Whether the configured model can generate reviewed managed Skill Candidates.
        external_skill_registry:
          type: boolean
          default: false
          description: Whether host-local external Skill discovery and exact resolution are configured.
        handoff_generation:
          type: boolean
          description: Whether exact evidence can be generated into an inspectable Handoff Draft.
        search_modes:
          type: array
          items:
            $ref: "#/components/schemas/MemorySearchMode"
        context_versions:
          type: array
          items:
            $ref: "#/components/schemas/PreparedContextSchema"
    FamilyCount:
      type: object
      additionalProperties: false
      required: [family, total]
      properties:
        family:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        total:
          type: integer
          minimum: 0
    CandidateFamilyCount:
      type: object
      additionalProperties: false
      required: [family, total, pending, approved, rejected]
      properties:
        family:
          $ref: "#/components/schemas/CandidateFamily"
        total:
          type: integer
          minimum: 0
        pending:
          type: integer
          minimum: 0
        approved:
          type: integer
          minimum: 0
        rejected:
          type: integer
          minimum: 0
    MemoryKindCount:
      type: object
      additionalProperties: false
      required: [kind, total, active, inactive]
      properties:
        kind:
          type: string
          minLength: 1
          maxLength: 128
        total:
          type: integer
          minimum: 0
        active:
          type: integer
          minimum: 0
        inactive:
          type: integer
          minimum: 0
    SourceInventoryStatistics:
      type: object
      additionalProperties: false
      required: [total, memory_processed, memory_pending]
      properties:
        total:
          type: integer
          minimum: 0
        memory_processed:
          type: integer
          minimum: 0
        memory_pending:
          type: integer
          minimum: 0
    ArtifactInventoryStatistics:
      type: object
      additionalProperties: false
      required: [total, by_family]
      properties:
        total:
          type: integer
          minimum: 0
        by_family:
          type: array
          items:
            $ref: "#/components/schemas/FamilyCount"
    CandidateInventoryStatistics:
      type: object
      additionalProperties: false
      required: [total, pending, approved, rejected, by_family]
      properties:
        total:
          type: integer
          minimum: 0
        pending:
          type: integer
          minimum: 0
        approved:
          type: integer
          minimum: 0
        rejected:
          type: integer
          minimum: 0
        by_family:
          type: array
          items:
            $ref: "#/components/schemas/CandidateFamilyCount"
    MemoryEntryInventoryStatistics:
      type: object
      additionalProperties: false
      required: [total, active, inactive, by_kind]
      properties:
        total:
          type: integer
          minimum: 0
        active:
          type: integer
          minimum: 0
        inactive:
          type: integer
          minimum: 0
        by_kind:
          type: array
          items:
            $ref: "#/components/schemas/MemoryKindCount"
    MemoryInventoryStatistics:
      type: object
      additionalProperties: false
      required: [entries]
      properties:
        entries:
          $ref: "#/components/schemas/MemoryEntryInventoryStatistics"
    InventoryStatistics:
      type: object
      additionalProperties: false
      required: [sources, artifacts, candidates, memory]
      properties:
        sources:
          $ref: "#/components/schemas/SourceInventoryStatistics"
        artifacts:
          $ref: "#/components/schemas/ArtifactInventoryStatistics"
        candidates:
          $ref: "#/components/schemas/CandidateInventoryStatistics"
        memory:
          $ref: "#/components/schemas/MemoryInventoryStatistics"
    ModelUsageValue:
      type: object
      additionalProperties: false
      required: [requests, input_tokens, output_tokens]
      properties:
        requests:
          type: integer
          minimum: 0
        input_tokens:
          type: integer
          minimum: 0
          nullable: true
        output_tokens:
          type: integer
          minimum: 0
          nullable: true
    ModelUsageStatistics:
      type: object
      additionalProperties: false
      required: [generation, embedding]
      properties:
        generation:
          $ref: "#/components/schemas/ModelUsageValue"
        embedding:
          $ref: "#/components/schemas/ModelUsageValue"
    ModelUsagePurposeBreakdown:
      type: object
      additionalProperties: false
      required: [purpose, generation, embedding]
      properties:
        purpose:
          type: string
          minLength: 1
          maxLength: 64
        generation:
          $ref: "#/components/schemas/ModelUsageValue"
        embedding:
          $ref: "#/components/schemas/ModelUsageValue"
    ModelUsageDay:
      type: object
      additionalProperties: false
      required: [date, generation, embedding, by_purpose]
      properties:
        date:
          type: string
          format: date
        generation:
          $ref: "#/components/schemas/ModelUsageValue"
        embedding:
          $ref: "#/components/schemas/ModelUsageValue"
        by_purpose:
          type: array
          maxItems: 16
          items:
            $ref: "#/components/schemas/ModelUsagePurposeBreakdown"
    ResolvedUsagePeriod:
      type: object
      additionalProperties: false
      required: [preset, start_date, end_date, timezone]
      properties:
        preset:
          $ref: "#/components/schemas/StatsPeriod"
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
        timezone:
          type: string
          enum: [UTC]
    UsageStatistics:
      type: object
      additionalProperties: false
      required: [period, totals, by_purpose, daily]
      properties:
        period:
          $ref: "#/components/schemas/ResolvedUsagePeriod"
        totals:
          $ref: "#/components/schemas/ModelUsageStatistics"
        by_purpose:
          type: array
          maxItems: 16
          items:
            $ref: "#/components/schemas/ModelUsagePurposeBreakdown"
        daily:
          type: array
          maxItems: 30
          items:
            $ref: "#/components/schemas/ModelUsageDay"
    TokenEstimatorProfile:
      type: object
      additionalProperties: false
      required: [estimator_id, version]
      properties:
        estimator_id:
          type: string
          minLength: 1
          maxLength: 128
        version:
          type: string
          minLength: 1
          maxLength: 64
    RecallTokenValue:
      type: object
      additionalProperties: false
      required: [preparations, ready_preparations, comparable_preparations, baseline_tokens, recalled_tokens, token_reduction]
      properties:
        preparations:
          type: integer
          minimum: 0
        ready_preparations:
          type: integer
          minimum: 0
        comparable_preparations:
          type: integer
          minimum: 0
        baseline_tokens:
          type: integer
          minimum: 0
        recalled_tokens:
          type: integer
          minimum: 0
        token_reduction:
          type: integer
    RecallTokenDay:
      type: object
      additionalProperties: false
      required: [date, preparations, ready_preparations, comparable_preparations, baseline_tokens, recalled_tokens, token_reduction]
      properties:
        date:
          type: string
          format: date
        preparations:
          type: integer
          minimum: 0
        ready_preparations:
          type: integer
          minimum: 0
        comparable_preparations:
          type: integer
          minimum: 0
        baseline_tokens:
          type: integer
          minimum: 0
        recalled_tokens:
          type: integer
          minimum: 0
        token_reduction:
          type: integer
    RecallTokenStatistics:
      type: object
      additionalProperties: false
      required: [period, estimator, totals, daily]
      properties:
        period:
          $ref: "#/components/schemas/ResolvedUsagePeriod"
        estimator:
          $ref: "#/components/schemas/TokenEstimatorProfile"
          nullable: true
        totals:
          $ref: "#/components/schemas/RecallTokenValue"
        daily:
          type: array
          maxItems: 30
          items:
            $ref: "#/components/schemas/RecallTokenDay"
    ScopeStats:
      type: object
      additionalProperties: false
      required: [scope_id, inventory, usage, recall]
      properties:
        scope_id:
          type: string
        inventory:
          $ref: "#/components/schemas/InventoryStatistics"
        usage:
          $ref: "#/components/schemas/UsageStatistics"
        recall:
          $ref: "#/components/schemas/RecallTokenStatistics"
    ScopedStats:
      type: object
      additionalProperties: false
      required: [selection, scope_ids, as_of, inventory, usage, recall, by_scope]
      properties:
        selection:
          $ref: "#/components/schemas/ScopeSelection"
        scope_ids:
          type: array
          uniqueItems: true
          items:
            type: string
        as_of:
          type: string
          format: date-time
        inventory:
          $ref: "#/components/schemas/InventoryStatistics"
        usage:
          $ref: "#/components/schemas/UsageStatistics"
        recall:
          $ref: "#/components/schemas/RecallTokenStatistics"
        by_scope:
          type: array
          items:
            $ref: "#/components/schemas/ScopeStats"
    GetStatsRequest:
      type: object
      additionalProperties: false
      required: [selection]
      properties:
        selection:
          $ref: "#/components/schemas/ScopeSelection"
        period:
          $ref: "#/components/schemas/StatsPeriod"
          default: 30d
    WorkClaimBasis:
      type: string
      enum: [declared, verified]
    WorkClaim:
      type: object
      additionalProperties: false
      required: [text, basis, evidence]
      properties:
        text:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
        basis:
          $ref: "#/components/schemas/WorkClaimBasis"
        evidence:
          type: array
          maxItems: 31
          items:
            $ref: "#/components/schemas/HandoffCitation"
    WorkContract:
      type: object
      additionalProperties: false
      required: [schema, trust, objective, facts, in_scope, exclusions, completion_criteria, authorization_notes, open_questions]
      properties:
        schema:
          type: string
          enum: [powercontext.work-contract.v1]
        trust:
          type: string
          enum: [untrusted_input]
        objective:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
        facts:
          type: array
          maxItems: 64
          items:
            $ref: "#/components/schemas/WorkClaim"
        in_scope:
          type: array
          minItems: 1
          maxItems: 64
          items:
            type: string
            minLength: 1
            maxLength: 8192
            pattern: '.*\S.*'
        exclusions:
          type: array
          maxItems: 64
          items:
            type: string
            minLength: 1
            maxLength: 8192
            pattern: '.*\S.*'
        completion_criteria:
          type: array
          minItems: 1
          maxItems: 64
          items:
            type: string
            minLength: 1
            maxLength: 8192
            pattern: '.*\S.*'
        authorization_notes:
          type: array
          maxItems: 64
          items:
            type: string
            minLength: 1
            maxLength: 8192
            pattern: '.*\S.*'
        open_questions:
          type: array
          maxItems: 64
          items:
            type: string
            minLength: 1
            maxLength: 8192
            pattern: '.*\S.*'
    CreateWorkContractRequest:
      type: object
      additionalProperties: false
      required: [scope_id, source_id, contract]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        source_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        contract:
          $ref: "#/components/schemas/WorkContract"
    CurrentWorkHandoff:
      type: object
      additionalProperties: false
      required: [schema, trust, objective, state, disposition, next_action, omissions]
      properties:
        schema:
          type: string
          enum: [powercontext.current-work-handoff.v1]
        trust:
          type: string
          enum: [untrusted_input]
        objective:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
        state:
          type: array
          minItems: 1
          maxItems: 64
          items:
            $ref: "#/components/schemas/WorkClaim"
        disposition:
          $ref: "#/components/schemas/HandoffDisposition"
        next_action:
          $ref: "#/components/schemas/WorkClaim"
          nullable: true
        omissions:
          type: array
          maxItems: 64
          items:
            type: string
            minLength: 1
            maxLength: 8192
            pattern: '.*\S.*'
    HandoffCurrentWorkRequest:
      type: object
      additionalProperties: false
      required: [scope_id, source_id, handoff]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        source_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        handoff:
          $ref: "#/components/schemas/CurrentWorkHandoff"
    WorkSourceKind:
      type: string
      enum: [work-contract, handoff-boundary, handoff-receipt, task-outcome]
    WorkSourceReceipt:
      type: object
      additionalProperties: false
      required: [kind, source, position, content_digest]
      properties:
        kind:
          $ref: "#/components/schemas/WorkSourceKind"
        source:
          $ref: "#/components/schemas/SourceReference"
        position:
          type: integer
          minimum: 1
        content_digest:
          type: string
          minLength: 71
          maxLength: 71
          pattern: '^sha256:[0-9a-f]{64}$'
    PreparedWorkHandoff:
      type: object
      additionalProperties: false
      required: [boundary, handoff]
      properties:
        boundary:
          $ref: "#/components/schemas/WorkSourceReceipt"
        handoff:
          $ref: "#/components/schemas/PreparedHandoff"
    HandoffReceiptStatus:
      type: string
      enum: [accepted, needs_clarification, declined]
    HandoffAcknowledgementSelection:
      type: string
      enum: [prepared, exact]
    LiveStateCheckStatus:
      type: string
      enum: [confirmed, mismatch, not_checked]
    ReceiverReadinessCheckStatus:
      type: string
      enum: [confirmed, insufficient, not_checked]
    ReceiverChecks:
      type: object
      additionalProperties: false
      description: Untrusted receiver self-attestation kept separate from citation availability. All three values must be confirmed when status is accepted.
      required: [live_state, capability, authorization]
      properties:
        live_state:
          $ref: "#/components/schemas/LiveStateCheckStatus"
        capability:
          $ref: "#/components/schemas/ReceiverReadinessCheckStatus"
        authorization:
          $ref: "#/components/schemas/ReceiverReadinessCheckStatus"
    AcknowledgeHandoffRequest:
      type: object
      additionalProperties: false
      required: [scope_id, source_id, receiver, status, selection]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        source_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        receiver:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        status:
          $ref: "#/components/schemas/HandoffReceiptStatus"
        selection:
          $ref: "#/components/schemas/HandoffAcknowledgementSelection"
        receiver_checks:
          $ref: "#/components/schemas/ReceiverChecks"
          nullable: true
        prepared:
          $ref: "#/components/schemas/PreparedHandoff"
          nullable: true
        revision:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        message:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
          nullable: true
    HandoffAcknowledgement:
      type: object
      additionalProperties: false
      required: [resolution, receipt]
      properties:
        receipt_identity:
          $ref: "#/components/schemas/HandoffReceiptIdentity"
          nullable: true
        resolution:
          $ref: "#/components/schemas/HandoffResolution"
        receipt:
          $ref: "#/components/schemas/WorkSourceReceipt"
    TaskOutcomeStatus:
      type: string
      enum: [succeeded, partial, blocked, failed, cancelled, unknown]
    TaskCheckStatus:
      type: string
      enum: [passed, failed, skipped, timed_out, unavailable, cancelled, unknown]
    TaskCheck:
      type: object
      additionalProperties: false
      required: [name, status, basis, evidence]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
        status:
          $ref: "#/components/schemas/TaskCheckStatus"
        details:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
          nullable: true
        basis:
          $ref: "#/components/schemas/WorkClaimBasis"
        evidence:
          type: array
          maxItems: 32
          items:
            $ref: "#/components/schemas/HandoffCitation"
    TaskOutcome:
      type: object
      additionalProperties: false
      required: [schema, trust, objective, status, summary, observations, checks, produced_artifacts, remaining_work]
      properties:
        schema:
          type: string
          enum: [powercontext.task-outcome.v1]
        trust:
          type: string
          enum: [untrusted_observation]
        objective:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
        status:
          $ref: "#/components/schemas/TaskOutcomeStatus"
        summary:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
        handoff_receipt_ref:
          $ref: "#/components/schemas/SourceReference"
          nullable: true
        observations:
          type: array
          minItems: 1
          maxItems: 64
          items:
            $ref: "#/components/schemas/WorkClaim"
        checks:
          type: array
          maxItems: 64
          items:
            $ref: "#/components/schemas/TaskCheck"
        produced_artifacts:
          type: array
          maxItems: 32
          items:
            $ref: "#/components/schemas/ArtifactReference"
        remaining_work:
          type: array
          maxItems: 64
          items:
            type: string
            minLength: 1
            maxLength: 8192
            pattern: '.*\S.*'
    RecordTaskOutcomeRequest:
      type: object
      additionalProperties: false
      required: [scope_id, source_id, outcome]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        source_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        outcome:
          $ref: "#/components/schemas/TaskOutcome"
    CaptureContentSourceRequest:
      type: object
      additionalProperties: false
      required: [scope_id, source_id, content]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        source_id:
          type: string
          minLength: 1
          maxLength: 256
        content:
          type: string
          minLength: 1
          maxLength: 200000
        metadata:
          type: object
          additionalProperties: true
          nullable: true
    CaptureContentSourceResponse:
      type: object
      additionalProperties: false
      required: [status, source, position]
      properties:
        status:
          $ref: "#/components/schemas/CaptureStatus"
        source:
          $ref: "#/components/schemas/SourceReference"
        position:
          type: integer
          minimum: 1
    SourceProjectionKey:
      type: object
      additionalProperties: false
      required: [name, version]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
        version:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
    SourceProjectionManifest:
      type: object
      additionalProperties: false
      required: [key, schema]
      properties:
        key:
          $ref: "#/components/schemas/SourceProjectionKey"
        schema:
          type: object
          additionalProperties: true
    SourceDefinitionManifest:
      type: object
      additionalProperties: false
      required: [name, version, fingerprint, source_schema, projections]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
        version:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
        fingerprint:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        source_schema:
          type: object
          additionalProperties: true
        projections:
          type: array
          maxItems: 16
          items:
            $ref: "#/components/schemas/SourceProjectionManifest"
    RegisterSourceDefinitionRequest:
      type: object
      additionalProperties: false
      required: [manifest]
      properties:
        manifest:
          $ref: "#/components/schemas/SourceDefinitionManifest"
    ConnectorBinding:
      type: object
      additionalProperties: false
      required: [scope_id, binding_id, connector_name, connector_version]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        binding_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        connector_name:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
        connector_version:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
    GetConnectorCheckpointRequest:
      type: object
      additionalProperties: false
      required: [binding]
      properties:
        binding:
          $ref: "#/components/schemas/ConnectorBinding"
    ConnectorCheckpointState:
      type: object
      additionalProperties: false
      required: [binding, checkpoint]
      properties:
        binding:
          $ref: "#/components/schemas/ConnectorBinding"
        checkpoint:
          nullable: true
    SourceProjectionValue:
      type: object
      additionalProperties: false
      required: [key, value]
      properties:
        key:
          $ref: "#/components/schemas/SourceProjectionKey"
        value: {}
    SourceObservation:
      type: object
      additionalProperties: false
      required:
        [name, definition_version, materialization, source_type, definition_fingerprint, payload, projections]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 256
        definition_version:
          type: string
          minLength: 1
          maxLength: 128
        materialization:
          type: string
          enum: [captured]
        description:
          type: string
          nullable: true
        source_type:
          type: string
          minLength: 1
          maxLength: 128
        definition_fingerprint:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        payload:
          type: object
          additionalProperties: true
        projections:
          type: array
          maxItems: 16
          items:
            $ref: "#/components/schemas/SourceProjectionValue"
    SubmitSourceObservationRequest:
      type: object
      additionalProperties: false
      required: [scope_id, observation]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        observation:
          $ref: "#/components/schemas/SourceObservation"
    SourceObservationReceipt:
      type: object
      additionalProperties: false
      required: [source, position]
      properties:
        source:
          $ref: "#/components/schemas/SourceReference"
        position:
          type: integer
          minimum: 1
    CommitConnectorCheckpointRequest:
      type: object
      additionalProperties: false
      required: [binding, expected, checkpoint]
      properties:
        binding:
          $ref: "#/components/schemas/ConnectorBinding"
        expected:
          nullable: true
        checkpoint:
          nullable: true
    CommitHandoffRequest:
      type: object
      additionalProperties: false
      required: [scope_id, handoff]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        handoff:
          $ref: "#/components/schemas/PreparedHandoff"
    CommittedHandoff:
      type: object
      additionalProperties: false
      required: [reference, content, source_refs, artifact_refs]
      properties:
        reference:
          $ref: "#/components/schemas/ArtifactReference"
        content:
          $ref: "#/components/schemas/HandoffContent"
        source_refs:
          type: array
          items:
            $ref: "#/components/schemas/SourceReference"
        artifact_refs:
          type: array
          items:
            $ref: "#/components/schemas/ArtifactReference"
    ContinueHandoffRequest:
      type: object
      additionalProperties: false
      required: [scope_id, selection]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        selection:
          $ref: "#/components/schemas/HandoffSelection"
        prepared:
          $ref: "#/components/schemas/PreparedHandoff"
          nullable: true
        revision:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
    FinalizeHandoffRequest:
      type: object
      additionalProperties: false
      required: [scope_id, draft]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        draft:
          $ref: "#/components/schemas/HandoffDraft"
    HandoffArtifactCitation:
      type: object
      additionalProperties: false
      required: [kind, artifact_ref]
      properties:
        kind:
          type: string
          enum: [artifact]
        artifact_ref:
          $ref: "#/components/schemas/ArtifactReference"
    HandoffActivation:
      type: object
      additionalProperties: false
      required: [status, boundary_source, previous_position, current_position, draft]
      properties:
        status:
          $ref: "#/components/schemas/HandoffActivationStatus"
        boundary_source:
          $ref: "#/components/schemas/SourceReference"
        previous_position:
          type: integer
          minimum: 0
        current_position:
          type: integer
          minimum: 0
        draft:
          $ref: "#/components/schemas/HandoffDraft"
          nullable: true
    HandoffCitation:
      oneOf:
        - $ref: "#/components/schemas/HandoffSourceCitation"
        - $ref: "#/components/schemas/HandoffArtifactCitation"
        - $ref: "#/components/schemas/HandoffMemoryCitation"
      discriminator:
        propertyName: kind
        mapping:
          source: "#/components/schemas/HandoffSourceCitation"
          artifact: "#/components/schemas/HandoffArtifactCitation"
          memory: "#/components/schemas/HandoffMemoryCitation"
    HandoffContent:
      type: object
      additionalProperties: false
      required: [schema, objective, state, disposition, next_action, omissions]
      properties:
        schema:
          $ref: "#/components/schemas/HandoffSchema"
        objective:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
        state:
          type: array
          minItems: 1
          maxItems: 64
          items:
            $ref: "#/components/schemas/HandoffStatement"
        disposition:
          $ref: "#/components/schemas/HandoffDisposition"
        next_action:
          $ref: "#/components/schemas/HandoffStatement"
          nullable: true
        omissions:
          type: array
          maxItems: 64
          items:
            $ref: "#/components/schemas/HandoffOmission"
    HandoffDraft:
      type: object
      additionalProperties: false
      required: [objective, state, disposition, next_action, omissions]
      properties:
        objective:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
        state:
          type: array
          minItems: 1
          maxItems: 64
          items:
            $ref: "#/components/schemas/HandoffStatement"
        disposition:
          $ref: "#/components/schemas/HandoffDisposition"
        next_action:
          $ref: "#/components/schemas/HandoffStatement"
          nullable: true
        omissions:
          type: array
          maxItems: 64
          items:
            $ref: "#/components/schemas/HandoffOmission"
    HandoffEvidenceCheck:
      type: object
      additionalProperties: false
      required: [claim, state_index, status, unavailable_evidence]
      properties:
        claim:
          $ref: "#/components/schemas/HandoffClaim"
        state_index:
          type: integer
          minimum: 0
          nullable: true
        status:
          $ref: "#/components/schemas/HandoffEvidenceStatus"
        unavailable_evidence:
          type: array
          maxItems: 32
          items:
            $ref: "#/components/schemas/HandoffCitation"
    HandoffMemoryCitation:
      type: object
      additionalProperties: false
      required: [kind, memory_citation]
      properties:
        kind:
          type: string
          enum: [memory]
        memory_citation:
          $ref: "#/components/schemas/MemoryCitation"
    HandoffOmission:
      type: object
      additionalProperties: false
      required: [text, citation]
      properties:
        text:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
        citation:
          $ref: "#/components/schemas/HandoffCitation"
          nullable: true
    HandoffResolution:
      type: object
      additionalProperties: false
      required:
        [trust, status, scope_id, content, selection, selected_revision, current_revision, evidence_checks]
      properties:
        trust:
          type: string
          enum: [untrusted_history]
        status:
          $ref: "#/components/schemas/HandoffResolutionStatus"
        scope_id:
          type: string
        content:
          $ref: "#/components/schemas/HandoffContent"
          nullable: true
        selection:
          $ref: "#/components/schemas/HandoffSelection"
          nullable: true
        selected_revision:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        current_revision:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        evidence_checks:
          type: array
          maxItems: 65
          items:
            $ref: "#/components/schemas/HandoffEvidenceCheck"
    HandoffSourceCitation:
      type: object
      additionalProperties: false
      required: [kind, source_ref]
      properties:
        kind:
          type: string
          enum: [source]
        source_ref:
          $ref: "#/components/schemas/SourceReference"
    HandoffStatement:
      type: object
      additionalProperties: false
      required: [text, citations]
      properties:
        text:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
        citations:
          type: array
          minItems: 1
          maxItems: 32
          items:
            $ref: "#/components/schemas/HandoffCitation"
    PrepareHandoffRequest:
      type: object
      additionalProperties: false
      required: [scope_id, objective, evidence]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        objective:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
        evidence:
          type: array
          minItems: 1
          maxItems: 32
          items:
            $ref: "#/components/schemas/HandoffCitation"
        max_bytes:
          type: integer
          minimum: 512
          maximum: 32768
          default: 8000
    PreparedHandoff:
      type: object
      additionalProperties: false
      required: [schema, scope_id, base, content]
      properties:
        schema:
          $ref: "#/components/schemas/PreparedHandoffSchema"
        scope_id:
          type: string
        base:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        content:
          $ref: "#/components/schemas/HandoffContent"
    PreparedContext:
      type: object
      additionalProperties: false
      required: [schema, status, content, content_bytes]
      properties:
        schema:
          $ref: "#/components/schemas/PreparedContextSchema"
        status:
          $ref: "#/components/schemas/PreparedContextStatus"
        content:
          type: string
          nullable: true
        content_bytes:
          type: integer
          minimum: 0
    EntryChange:
      type: object
      additionalProperties: false
      required: [op, entry_id, from_entry_version_id, to_entry_version_id, reason]
      properties:
        op:
          $ref: "#/components/schemas/EntryChangeOperation"
        entry_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        from_entry_version_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
          nullable: true
        to_entry_version_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
          nullable: true
        reason:
          type: string
          nullable: true
    ExperienceArtifact:
      type: object
      additionalProperties: false
      required: [artifact, content, source_refs, artifact_refs]
      properties:
        artifact:
          $ref: "#/components/schemas/ArtifactReference"
        content:
          $ref: "#/components/schemas/ExperienceProposal"
        source_refs:
          type: array
          items:
            $ref: "#/components/schemas/SourceReference"
        artifact_refs:
          type: array
          items:
            $ref: "#/components/schemas/ArtifactReference"
    ExperienceProposal:
      type: object
      additionalProperties: false
      required: [situation, action, outcome, lesson]
      properties:
        situation:
          type: string
          minLength: 1
          maxLength: 8000
          pattern: '.*\S.*'
        action:
          type: string
          minLength: 1
          maxLength: 8000
          pattern: '.*\S.*'
        outcome:
          type: string
          minLength: 1
          maxLength: 8000
          pattern: '.*\S.*'
        lesson:
          type: string
          minLength: 1
          maxLength: 8000
          pattern: '.*\S.*'
    SkillArtifact:
      type: object
      additionalProperties: false
      required: [artifact, content, source_refs, artifact_refs]
      properties:
        artifact:
          $ref: "#/components/schemas/ArtifactReference"
        content:
          $ref: "#/components/schemas/SkillProposal"
        source_refs:
          type: array
          items:
            $ref: "#/components/schemas/SourceReference"
        artifact_refs:
          type: array
          items:
            $ref: "#/components/schemas/ArtifactReference"
    SkillLifecycleState:
      type: string
      enum: [active, deprecated, retired]
    SkillGovernance:
      type: object
      additionalProperties: false
      required: [artifact, lifecycle_state, replacement_artifact_id, governance_generation]
      properties:
        artifact:
          $ref: "#/components/schemas/ArtifactReference"
        lifecycle_state:
          $ref: "#/components/schemas/SkillLifecycleState"
        replacement_artifact_id:
          type: string
          minLength: 1
          maxLength: 128
          nullable: true
        governance_generation:
          type: integer
          minimum: 0
    ManagedSkillLibraryEntry:
      type: object
      additionalProperties: false
      required: [artifact, content, source_refs, artifact_refs, governance]
      properties:
        artifact:
          $ref: "#/components/schemas/ArtifactReference"
        content:
          $ref: "#/components/schemas/SkillProposal"
        source_refs:
          type: array
          items:
            $ref: "#/components/schemas/SourceReference"
        artifact_refs:
          type: array
          items:
            $ref: "#/components/schemas/ArtifactReference"
        governance:
          $ref: "#/components/schemas/SkillGovernance"
    ListManagedSkillsRequest:
      type: object
      additionalProperties: false
      required: [scope_id]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        query:
          type: string
          minLength: 1
          maxLength: 2000
          nullable: true
        include_deprecated:
          type: boolean
          default: false
        limit:
          type: integer
          minimum: 1
          maximum: 200
          default: 100
    ListManagedSkillsResponse:
      type: object
      additionalProperties: false
      required: [skills]
      properties:
        skills:
          type: array
          maxItems: 200
          items:
            $ref: "#/components/schemas/ManagedSkillLibraryEntry"
    UpdateSkillLifecycleRequest:
      type: object
      additionalProperties: false
      required: [scope_id, artifact_id, expected_generation, lifecycle_state]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        artifact_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        expected_generation:
          type: integer
          minimum: 0
        lifecycle_state:
          $ref: "#/components/schemas/SkillLifecycleState"
        replacement_artifact_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
          nullable: true
    SkillProposal:
      type: object
      additionalProperties: false
      required: [name, description, instructions, validation]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^\S(?:.*\S)?$'
        description:
          type: string
          minLength: 1
          maxLength: 2000
          pattern: '^\S(?:.*\S)?$'
        instructions:
          type: string
          maxLength: 131072
        validation:
          type: array
          maxItems: 32
          items:
            $ref: "#/components/schemas/SkillValidationItem"
        package:
          $ref: "#/components/schemas/SkillPackageReference"
          nullable: true
        license:
          type: string
          minLength: 1
          maxLength: 512
          nullable: true
        compatibility:
          type: string
          minLength: 1
          maxLength: 500
          nullable: true
        metadata:
          type: object
          maxProperties: 64
          additionalProperties:
            type: string
        allowed_tools:
          type: string
          minLength: 1
          maxLength: 2000
          nullable: true
    SkillPackageReference:
      type: object
      additionalProperties: false
      required: [tree_digest, archive_digest, file_count, uncompressed_size, archive_size]
      properties:
        tree_digest:
          type: string
          pattern: '^[0-9a-f]{64}$'
        archive_digest:
          type: string
          pattern: '^[0-9a-f]{64}$'
        file_count:
          type: integer
          minimum: 1
          maximum: 256
        uncompressed_size:
          type: integer
          minimum: 1
          maximum: 4194304
        archive_size:
          type: integer
          minimum: 1
          maximum: 5242880
    SkillPackageFile:
      type: object
      additionalProperties: false
      required: [path, digest, size, media_type, executable]
      properties:
        path:
          type: string
          minLength: 1
          maxLength: 512
        digest:
          type: string
          pattern: '^[0-9a-f]{64}$'
        size:
          type: integer
          minimum: 0
          maximum: 4194304
        media_type:
          type: string
          minLength: 1
          maxLength: 255
        executable:
          type: boolean
    SkillPackageManifest:
      type: object
      additionalProperties: false
      required: [package, name, description, metadata, files]
      properties:
        package:
          $ref: "#/components/schemas/SkillPackageReference"
        name:
          type: string
          minLength: 1
          maxLength: 64
        description:
          type: string
          minLength: 1
          maxLength: 1024
        license:
          type: string
          minLength: 1
          maxLength: 512
          nullable: true
        compatibility:
          type: string
          minLength: 1
          maxLength: 500
          nullable: true
        metadata:
          type: object
          maxProperties: 64
          additionalProperties:
            type: string
        allowed_tools:
          type: string
          minLength: 1
          maxLength: 2000
          nullable: true
        files:
          type: array
          minItems: 1
          maxItems: 256
          items:
            $ref: "#/components/schemas/SkillPackageFile"
    GetSkillPackageRequest:
      type: object
      additionalProperties: false
      required: [scope_id, artifact]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        artifact:
          $ref: "#/components/schemas/ArtifactReference"
    SkillPackageDownload:
      type: object
      additionalProperties: false
      required: [package, archive_base64]
      properties:
        package:
          $ref: "#/components/schemas/SkillPackageReference"
        archive_base64:
          type: string
          minLength: 1
          maxLength: 6990508
          pattern: '^[A-Za-z0-9+/]*={0,2}$'
    RemoteAgentKind:
      type: string
      enum: [codex, claude_code]
    RemoteSkillTargetState:
      type: string
      enum: [pending, active, revoked]
    RemoteSkillTarget:
      type: object
      additionalProperties: false
      required:
        - scope_id
        - target_id
        - display_name
        - agent_kind
        - installation_scope
        - delivery_mode
        - installation_id
        - state
        - receiver_version
        - environment_fingerprint
        - machine_hostname
        - workspace_name
        - last_seen_at
        - generation
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
        target_id:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
        display_name:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
        agent_kind:
          $ref: "#/components/schemas/RemoteAgentKind"
        installation_scope:
          type: string
          enum: [project]
        delivery_mode:
          type: string
          enum: [agent_pull]
        installation_id:
          type: string
          minLength: 1
          maxLength: 128
          nullable: true
        state:
          $ref: "#/components/schemas/RemoteSkillTargetState"
        receiver_version:
          type: string
          minLength: 1
          maxLength: 64
          nullable: true
        environment_fingerprint:
          type: string
          pattern: '^[0-9a-f]{64}$'
          nullable: true
        machine_hostname:
          type: string
          minLength: 1
          maxLength: 255
          pattern: '.*\S.*'
          nullable: true
        workspace_name:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
          nullable: true
        last_seen_at:
          type: string
          format: date-time
          nullable: true
        generation:
          type: integer
          minimum: 0
    ListRemoteSkillTargetsRequest:
      type: object
      additionalProperties: false
      required: [scope_id]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        target_id:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
          nullable: true
        limit:
          type: integer
          minimum: 1
          maximum: 200
          default: 100
    RemoteSkillTargetStatus:
      type: object
      additionalProperties: false
      required: [target, publications]
      properties:
        target:
          $ref: "#/components/schemas/RemoteSkillTarget"
        publications:
          type: array
          maxItems: 256
          items:
            $ref: "#/components/schemas/RemoteSkillPublication"
    ListRemoteSkillTargetsResponse:
      type: object
      additionalProperties: false
      required: [targets]
      properties:
        targets:
          type: array
          maxItems: 200
          items:
            $ref: "#/components/schemas/RemoteSkillTargetStatus"
    CreateRemoteSkillTargetRequest:
      type: object
      additionalProperties: false
      required: [scope_id, agent_kind, display_name]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        agent_kind:
          $ref: "#/components/schemas/RemoteAgentKind"
        display_name:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
    RemoteSkillTargetEnrollment:
      type: object
      additionalProperties: false
      required: [target, enrollment_code, enrollment_expires_at]
      properties:
        target:
          $ref: "#/components/schemas/RemoteSkillTarget"
        enrollment_code:
          type: string
          minLength: 32
          maxLength: 256
        enrollment_expires_at:
          type: string
          format: date-time
    EnrollRemoteSkillTargetRequest:
      type: object
      additionalProperties: false
      required: [enrollment_code, installation_id, receiver_version]
      properties:
        enrollment_code:
          type: string
          minLength: 32
          maxLength: 256
        installation_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        receiver_version:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '^[\x21-\x7E]+$'
        environment_fingerprint:
          type: string
          pattern: '^[0-9a-f]{64}$'
          nullable: true
        machine_hostname:
          type: string
          minLength: 1
          maxLength: 255
          pattern: '.*\S.*'
          nullable: true
        workspace_name:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
          nullable: true
    RemoteSkillTargetCredential:
      type: object
      additionalProperties: false
      required: [scope_id, target_id, agent_kind, credential]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
        target_id:
          type: string
          minLength: 1
          maxLength: 64
        agent_kind:
          $ref: "#/components/schemas/RemoteAgentKind"
        credential:
          type: string
          minLength: 32
          maxLength: 256
    RevokeRemoteSkillTargetRequest:
      type: object
      additionalProperties: false
      required: [scope_id, target_id, expected_generation]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        target_id:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
        expected_generation:
          type: integer
          minimum: 0
    RenameRemoteSkillTargetRequest:
      type: object
      additionalProperties: false
      required: [scope_id, target_id, display_name, expected_generation]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        target_id:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
        display_name:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
        expected_generation:
          type: integer
          minimum: 0
    PublishRemoteSkillRequest:
      type: object
      additionalProperties: false
      required: [scope_id, target_id, artifact, expected_generation]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        target_id:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
        artifact:
          $ref: "#/components/schemas/ArtifactReference"
        expected_generation:
          type: integer
          minimum: 0
          nullable: true
        allow_deprecated:
          type: boolean
          default: false
    UnpublishRemoteSkillRequest:
      type: object
      additionalProperties: false
      required: [scope_id, target_id, artifact_id, expected_generation]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        target_id:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
        artifact_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        expected_generation:
          type: integer
          minimum: 0
    RemoteSkillDesiredState:
      type: string
      enum: [published, unpublished]
    RemoteSkillPublicationState:
      type: string
      enum: [unpublished, pending, current, update_available, delivery_failed, conflict, drifted, incompatible]
    RemoteSkillPublication:
      type: object
      additionalProperties: false
      required:
        - scope_id
        - target_id
        - artifact_id
        - desired_state
        - desired_revision
        - desired_tree_digest
        - observed_revision
        - observed_tree_digest
        - observed_generation
        - state
        - last_error_code
        - observed_at
        - generation
      properties:
        scope_id:
          type: string
        target_id:
          type: string
        artifact_id:
          type: string
        desired_state:
          $ref: "#/components/schemas/RemoteSkillDesiredState"
        desired_revision:
          type: integer
          minimum: 1
        desired_tree_digest:
          type: string
          pattern: '^[0-9a-f]{64}$'
        observed_revision:
          type: integer
          minimum: 1
          nullable: true
        observed_tree_digest:
          type: string
          pattern: '^[0-9a-f]{64}$'
          nullable: true
        observed_generation:
          type: integer
          minimum: 0
          nullable: true
        state:
          $ref: "#/components/schemas/RemoteSkillPublicationState"
        last_error_code:
          type: string
          minLength: 1
          maxLength: 128
          nullable: true
        observed_at:
          type: string
          format: date-time
          nullable: true
        generation:
          type: integer
          minimum: 0
    RemoteSkillObservation:
      type: object
      additionalProperties: false
      required: [artifact, tree_digest, actual_tree_digest, skill_name, applied_generation]
      properties:
        artifact:
          $ref: "#/components/schemas/ArtifactReference"
        tree_digest:
          type: string
          pattern: '^[0-9a-f]{64}$'
        actual_tree_digest:
          type: string
          pattern: '^[0-9a-f]{64}$'
          nullable: true
        skill_name:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
        applied_generation:
          type: integer
          minimum: 0
    ReconcileRemoteSkillsRequest:
      type: object
      additionalProperties: false
      required: [observations, receiver_version]
      properties:
        observations:
          type: array
          maxItems: 256
          items:
            $ref: "#/components/schemas/RemoteSkillObservation"
        receiver_version:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '^[\x21-\x7E]+$'
        environment_fingerprint:
          type: string
          pattern: '^[0-9a-f]{64}$'
          nullable: true
    RemoteSkillOperation:
      type: string
      enum: [install, unpublish]
    RemoteSkillAction:
      type: object
      additionalProperties: false
      required:
        [operation, generation, artifact, tree_digest, skill_name, package, expected_local, blocked_error_code]
      properties:
        operation:
          $ref: "#/components/schemas/RemoteSkillOperation"
        generation:
          type: integer
          minimum: 0
        artifact:
          $ref: "#/components/schemas/ArtifactReference"
        tree_digest:
          type: string
          pattern: '^[0-9a-f]{64}$'
        skill_name:
          type: string
          minLength: 1
          maxLength: 64
        package:
          $ref: "#/components/schemas/SkillPackageReference"
          nullable: true
        expected_local:
          $ref: "#/components/schemas/RemoteSkillObservation"
          nullable: true
        blocked_error_code:
          type: string
          minLength: 1
          maxLength: 128
          nullable: true
    ReconcileRemoteSkillsResponse:
      type: object
      additionalProperties: false
      required: [scope_id, target_id, actions]
      properties:
        scope_id:
          type: string
        target_id:
          type: string
        actions:
          type: array
          maxItems: 256
          items:
            $ref: "#/components/schemas/RemoteSkillAction"
    DownloadRemoteSkillPackageRequest:
      type: object
      additionalProperties: false
      required: [generation, artifact, package]
      properties:
        generation:
          type: integer
          minimum: 0
        artifact:
          $ref: "#/components/schemas/ArtifactReference"
        package:
          $ref: "#/components/schemas/SkillPackageReference"
    RemoteSkillReceiptOutcome:
      type: string
      enum: [succeeded, failed]
    RemoteSkillFailureState:
      type: string
      enum: [delivery_failed, conflict, drifted, incompatible]
    RecordRemoteSkillReceiptRequest:
      type: object
      additionalProperties: false
      required:
        - operation
        - generation
        - artifact
        - expected_tree_digest
        - observed_tree_digest
        - outcome
        - failure_state
        - error_code
        - receiver_version
        - environment_fingerprint
      properties:
        operation:
          $ref: "#/components/schemas/RemoteSkillOperation"
        generation:
          type: integer
          minimum: 0
        artifact:
          $ref: "#/components/schemas/ArtifactReference"
        expected_tree_digest:
          type: string
          pattern: '^[0-9a-f]{64}$'
        observed_tree_digest:
          type: string
          pattern: '^[0-9a-f]{64}$'
          nullable: true
        outcome:
          $ref: "#/components/schemas/RemoteSkillReceiptOutcome"
        failure_state:
          $ref: "#/components/schemas/RemoteSkillFailureState"
          nullable: true
        error_code:
          type: string
          minLength: 1
          maxLength: 128
          nullable: true
        receiver_version:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '^[\x21-\x7E]+$'
        environment_fingerprint:
          type: string
          pattern: '^[0-9a-f]{64}$'
          nullable: true
    RemoteSkillReceiptResponse:
      type: object
      additionalProperties: false
      required: [accepted, stale, publication]
      properties:
        accepted:
          type: boolean
        stale:
          type: boolean
        publication:
          $ref: "#/components/schemas/RemoteSkillPublication"
    ProposeSkillPackageRequest:
      type: object
      additionalProperties: false
      required: [scope_id, archive_base64]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        archive_base64:
          type: string
          minLength: 1
          maxLength: 6990508
          pattern: '^[A-Za-z0-9+/]*={0,2}$'
        reason:
          type: string
          minLength: 1
          maxLength: 2000
          nullable: true
        target:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
          description: Exact managed Skill Revision replaced by this complete package Candidate.
    RecordSkillUsageRequest:
      type: object
      additionalProperties: false
      required:
        - scope_id
        - observation_id
        - skill_ref
        - package_digest
        - target_id
        - selected
        - invoked
        - validation
        - outcome
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        observation_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        skill_ref:
          $ref: "#/components/schemas/ArtifactReference"
        package_digest:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        target_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
        selected:
          type: boolean
        invoked:
          type: string
          enum: ['true', 'false', unknown]
        validation:
          type: string
          enum: [passed, failed, unknown]
        outcome:
          type: string
          enum: [success, failure, unknown]
        task_source:
          $ref: "#/components/schemas/SourceReference"
          nullable: true
        environment_fingerprint:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
          nullable: true
    SkillValidationItem:
      type: string
      minLength: 1
      maxLength: 2000
      pattern: '^\S(?:.*\S)?$'
    ExternalSkillRegistration:
      type: object
      additionalProperties: false
      required:
        - external_skill_id
        - provider
        - agent_kind
        - host_id
        - installation_scope
        - locator
        - fingerprint
        - name
        - description
      properties:
        external_skill_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        provider:
          type: string
          enum: [codex, claude_code]
        agent_kind:
          type: string
          enum: [codex, claude_code]
        host_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^\S(?:.*\S)?$'
        installation_scope:
          $ref: "#/components/schemas/ExternalSkillInstallationScope"
        locator:
          type: string
          minLength: 1
          maxLength: 2000
          pattern: '^\S(?:.*\S)?$'
          description: Host-local locator; not a cross-Agent or cross-host contract.
        fingerprint:
          type: string
          pattern: '^[0-9a-f]{64}$'
        name:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^\S(?:.*\S)?$'
        description:
          type: string
          minLength: 1
          maxLength: 2000
          pattern: '^\S(?:.*\S)?$'
    ExternalSkillResolution:
      type: object
      additionalProperties: false
      required: [registration, status, entrypoint]
      properties:
        registration:
          $ref: "#/components/schemas/ExternalSkillRegistration"
        status:
          $ref: "#/components/schemas/ExternalSkillResolutionStatus"
        entrypoint:
          type: string
          nullable: true
          description: Host-local SKILL.md path; present only when the exact fingerprint is available.
    ScanExternalSkillsResponse:
      type: object
      additionalProperties: false
      required: [registrations, skipped]
      properties:
        registrations:
          type: array
          items:
            $ref: "#/components/schemas/ExternalSkillRegistration"
        skipped:
          type: integer
          minimum: 0
    ListExternalSkillsResponse:
      type: object
      additionalProperties: false
      required: [skills]
      properties:
        skills:
          type: array
          items:
            $ref: "#/components/schemas/ExternalSkillResolution"
    ErrorDetail:
      type: object
      additionalProperties: false
      required: [code, message, details]
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: object
          additionalProperties: true
          nullable: true
    ErrorResponse:
      type: object
      additionalProperties: false
      required: [error]
      properties:
        error:
          $ref: "#/components/schemas/ErrorDetail"
    FlushMemoryRequest:
      type: object
      additionalProperties: false
      required: [scope_id]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
    FlushMemoryResponse:
      type: object
      additionalProperties: false
      required: [status, previous_cursor, current_cursor, high_watermark, processed_source_count]
      properties:
        status:
          $ref: "#/components/schemas/FlushStatus"
        previous_cursor:
          type: integer
          minimum: 0
        current_cursor:
          type: integer
          minimum: 0
        high_watermark:
          type: integer
          minimum: 0
        processed_source_count:
          type: integer
          minimum: 0
        memory:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
    GetMemoryEntryRequest:
      type: object
      additionalProperties: false
      required: [scope_id, citation]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        citation:
          $ref: "#/components/schemas/MemoryCitation"
    GetArtifactCandidateRequest:
      type: object
      additionalProperties: false
      required: [scope_id, candidate_id]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        candidate_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
    GetExperienceRequest:
      type: object
      additionalProperties: false
      required: [scope_id, artifact]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        artifact:
          $ref: "#/components/schemas/ArtifactReference"
    GetSkillRequest:
      type: object
      additionalProperties: false
      required: [scope_id, artifact]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        artifact:
          $ref: "#/components/schemas/ArtifactReference"
    GetHandoffReportRequest:
      type: object
      additionalProperties: false
      required: [selection]
      properties:
        selection:
          $ref: "#/components/schemas/ScopeSelection"
        format:
          $ref: "#/components/schemas/ReportFormat"
          default: json
        download:
          type: boolean
          default: false
    HandoffReportResponse:
      type: object
      additionalProperties: false
      required: [format, report, markdown, selection_digest, report_digest]
      properties:
        format:
          $ref: "#/components/schemas/ReportFormat"
        report:
          type: object
          additionalProperties: true
          nullable: true
        markdown:
          type: string
          nullable: true
        selection_digest:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        report_digest:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
    ReportFormat:
      type: string
      enum: [json, markdown]
    HealthResponse:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status:
          type: string
    ListMemoryChangesRequest:
      type: object
      additionalProperties: false
      required: [scope_id]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        since_revision:
          type: integer
          minimum: 0
          nullable: true
          description: >-
            Exclusive lower bound; 0 requests complete history from Revision 1.
            Positive nonexistent revisions are errors.
    ListMemoryChangesResponse:
      type: object
      additionalProperties: false
      required: [revisions]
      properties:
        memory:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        revisions:
          type: array
          items:
            $ref: "#/components/schemas/MemoryRevisionChanges"
    ListMemoryEntriesRequest:
      type: object
      additionalProperties: false
      required: [scope_id]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        include_inactive:
          type: boolean
          default: false
          description: Include inactive entries from the current Memory head for explicit audit.
    ListMemoryEntriesResponse:
      type: object
      additionalProperties: false
      required: [entries]
      properties:
        memory:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        entries:
          type: array
          items:
            $ref: "#/components/schemas/MemoryEntry"
    ListArtifactCandidatesRequest:
      type: object
      additionalProperties: false
      required: [scope_id]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        status:
          $ref: "#/components/schemas/CandidateStatus"
          default: pending
        family:
          $ref: "#/components/schemas/CandidateFamily"
          nullable: true
        cursor:
          type: string
          minLength: 1
          maxLength: 128
          nullable: true
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 50
    ListExternalSkillsRequest:
      type: object
      additionalProperties: false
      required: [scope_id]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        include_unavailable:
          type: boolean
          default: false
    MemoryEntry:
      type: object
      additionalProperties: false
      required:
        - citation
        - version
        - kind
        - text
        - state
        - source_refs
        - artifact_refs
      properties:
        citation:
          $ref: "#/components/schemas/MemoryCitation"
        version:
          type: integer
          minimum: 1
        kind:
          type: string
        text:
          type: string
        state:
          $ref: "#/components/schemas/MemoryEntryState"
        source_refs:
          type: array
          items:
            $ref: "#/components/schemas/SourceReference"
        artifact_refs:
          type: array
          items:
            $ref: "#/components/schemas/ArtifactReference"
    MemoryMutationResponse:
      type: object
      additionalProperties: false
      required: [memory]
      properties:
        memory:
          $ref: "#/components/schemas/ArtifactReference"
        entry:
          $ref: "#/components/schemas/MemoryEntry"
          nullable: true
    MemoryCitation:
      type: object
      additionalProperties: false
      required: [memory_ref, entry_id, entry_version_id]
      properties:
        memory_ref:
          $ref: "#/components/schemas/ArtifactReference"
        entry_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        entry_version_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
    MemoryRevisionChanges:
      type: object
      additionalProperties: false
      required: [memory_ref, changes]
      properties:
        memory_ref:
          $ref: "#/components/schemas/ArtifactReference"
        changes:
          type: array
          items:
            $ref: "#/components/schemas/EntryChange"
    PrepareContextRequest:
      type: object
      additionalProperties: false
      required: [scope_id, query]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        query:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
        max_bytes:
          type: integer
          minimum: 512
          maximum: 32768
          default: 8000
    ProposeExperienceRequest:
      type: object
      additionalProperties: false
      required: [scope_id, proposal, source_refs, artifact_refs]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        proposal:
          $ref: "#/components/schemas/ExperienceProposal"
        source_refs:
          type: array
          maxItems: 32
          description: Exact Source evidence. Counted with artifact_refs toward a combined maximum of 32 references.
          items:
            $ref: "#/components/schemas/SourceReference"
        artifact_refs:
          type: array
          maxItems: 32
          description: Exact Artifact evidence. Counted with source_refs toward a combined maximum of 32 references.
          items:
            $ref: "#/components/schemas/ArtifactReference"
        target:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        reason:
          type: string
          minLength: 1
          maxLength: 2000
          nullable: true
    GenerateExperienceRequest:
      type: object
      additionalProperties: false
      required: [scope_id, source_refs, artifact_refs]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        source_refs:
          type: array
          maxItems: 32
          description: Exact Source evidence. Counted with artifact_refs toward a combined maximum of 32 references.
          items:
            $ref: "#/components/schemas/SourceReference"
        artifact_refs:
          type: array
          maxItems: 32
          description: Exact Artifact evidence. Counted with source_refs toward a combined maximum of 32 references.
          items:
            $ref: "#/components/schemas/ArtifactReference"
        target:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        reason:
          type: string
          minLength: 1
          maxLength: 2000
          nullable: true
    ProposeSkillRequest:
      type: object
      additionalProperties: false
      required: [scope_id, proposal, source_refs, artifact_refs]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        proposal:
          $ref: "#/components/schemas/SkillProposal"
        source_refs:
          type: array
          maxItems: 32
          description: Exact Source evidence. Counted with artifact_refs toward a combined maximum of 32 references.
          items:
            $ref: "#/components/schemas/SourceReference"
        artifact_refs:
          type: array
          maxItems: 32
          description: Exact Artifact evidence. Counted with source_refs toward a combined maximum of 32 references.
          items:
            $ref: "#/components/schemas/ArtifactReference"
        target:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        reason:
          type: string
          minLength: 1
          maxLength: 2000
          nullable: true
    SkillGenerationOrigin:
      type: string
      enum: [experience, source, usage]
      description: The operation-specific direct provenance shape required for managed Skill generation.
    GenerateSkillRequest:
      type: object
      additionalProperties: false
      required: [scope_id, origin, source_refs, artifact_refs]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        origin:
          $ref: "#/components/schemas/SkillGenerationOrigin"
        source_refs:
          type: array
          maxItems: 32
          description: Exact Source evidence. Counted with artifact_refs toward a combined maximum of 32 references.
          items:
            $ref: "#/components/schemas/SourceReference"
        artifact_refs:
          type: array
          maxItems: 32
          description: Exact Artifact evidence. Counted with source_refs toward a combined maximum of 32 references.
          items:
            $ref: "#/components/schemas/ArtifactReference"
        target:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        reason:
          type: string
          minLength: 1
          maxLength: 2000
          nullable: true
    GeneratedCandidateStatus:
      type: string
      enum: [pending, no_op]
    GeneratedCandidateResponse:
      type: object
      additionalProperties: false
      required: [status, candidate]
      properties:
        status:
          $ref: "#/components/schemas/GeneratedCandidateStatus"
        candidate:
          $ref: "#/components/schemas/ArtifactCandidate"
          nullable: true
    ReadinessResponse:
      type: object
      additionalProperties: false
      required: [status, checks]
      properties:
        status:
          $ref: "#/components/schemas/ReadinessStatus"
        checks:
          type: object
          additionalProperties:
            type: string
    ReadinessStatus:
      type: string
      enum: [ready, degraded, not_ready]
    RememberMemoryRequest:
      type: object
      additionalProperties: false
      required: [scope_id, kind, text]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        kind:
          type: string
          minLength: 1
          maxLength: 128
        text:
          type: string
          minLength: 1
          description: Must not exceed 8192 UTF-8 bytes after normalization.
        reason:
          type: string
          maxLength: 512
          nullable: true
        expected_revision:
          type: integer
          minimum: 1
          nullable: true
    RetireMemoryEntryRequest:
      type: object
      additionalProperties: false
      required: [scope_id, citation]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        citation:
          $ref: "#/components/schemas/MemoryCitation"
        reason:
          type: string
          maxLength: 512
          nullable: true
    RejectArtifactCandidateRequest:
      type: object
      additionalProperties: false
      required: [scope_id, candidate_id, expected_version, reason]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        candidate_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        expected_version:
          type: integer
          minimum: 1
        reason:
          type: string
          minLength: 1
          maxLength: 2000
          pattern: '.*\S.*'
    ReviseArtifactCandidateRequest:
      type: object
      additionalProperties: false
      required: [scope_id, candidate_id, expected_version, proposal, source_refs, artifact_refs]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        candidate_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        expected_version:
          type: integer
          minimum: 1
        proposal:
          oneOf:
            - $ref: "#/components/schemas/ExperienceProposal"
            - $ref: "#/components/schemas/SkillProposal"
        source_refs:
          type: array
          maxItems: 32
          description: Exact Source evidence. Counted with artifact_refs toward a combined maximum of 32 references.
          items:
            $ref: "#/components/schemas/SourceReference"
        artifact_refs:
          type: array
          maxItems: 32
          description: Exact Artifact evidence. Counted with source_refs toward a combined maximum of 32 references.
          items:
            $ref: "#/components/schemas/ArtifactReference"
        target:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        reason:
          type: string
          minLength: 1
          maxLength: 2000
          nullable: true
    ReviseMemoryEntryRequest:
      type: object
      additionalProperties: false
      required: [scope_id, citation, kind, text]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        citation:
          $ref: "#/components/schemas/MemoryCitation"
        kind:
          type: string
          minLength: 1
          maxLength: 128
        text:
          type: string
          minLength: 1
          description: Must not exceed 8192 UTF-8 bytes after normalization.
        reason:
          type: string
          maxLength: 512
          nullable: true
    SearchMemoryHit:
      type: object
      additionalProperties: false
      required: [citation, text, score, matched_by]
      properties:
        citation:
          $ref: "#/components/schemas/MemoryCitation"
        text:
          type: string
        score:
          type: number
          minimum: 0
          maximum: 1
        matched_by:
          type: array
          items:
            $ref: "#/components/schemas/MemoryMatchedBy"
    SearchMemoryRequest:
      type: object
      additionalProperties: false
      required: [scope_id, query]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        query:
          type: string
          minLength: 1
          maxLength: 8192
        limit:
          type: integer
          minimum: 1
          maximum: 50
          default: 10
        mode:
          $ref: "#/components/schemas/MemorySearchMode"
          default: auto
    ScanExternalSkillsRequest:
      type: object
      additionalProperties: false
      required: [scope_id]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
    ResolveExternalSkillRequest:
      type: object
      additionalProperties: false
      required: [scope_id, external_skill_id, fingerprint]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        external_skill_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        fingerprint:
          type: string
          pattern: '^[0-9a-f]{64}$'
    ExternalSkillImportMode:
      type: string
      enum: [import, fork]
    ImportExternalSkillRequest:
      type: object
      additionalProperties: false
      required: [scope_id, external_skill_id, fingerprint, mode]
      properties:
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        external_skill_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
        fingerprint:
          type: string
          pattern: '^[0-9a-f]{64}$'
          description: Exact package fingerprint captured into Source lineage.
        mode:
          $ref: "#/components/schemas/ExternalSkillImportMode"
        reason:
          type: string
          minLength: 1
          maxLength: 2000
          nullable: true
    SearchMemoryResponse:
      type: object
      additionalProperties: false
      required: [hits]
      properties:
        memory:
          $ref: "#/components/schemas/ArtifactReference"
          nullable: true
        mode:
          $ref: "#/components/schemas/MemoryUsedSearchMode"
          nullable: true
        hits:
          type: array
          items:
            $ref: "#/components/schemas/SearchMemoryHit"
    CreateArtifactRequest:
      oneOf:
        - $ref: "#/components/schemas/CreateMemoryArtifactRequest"
        - $ref: "#/components/schemas/CreateExperienceArtifactRequest"
        - $ref: "#/components/schemas/CreateSkillArtifactRequest"
        - $ref: "#/components/schemas/CreateHandoffArtifactRequest"
      discriminator:
        propertyName: family
        mapping:
          memory: "#/components/schemas/CreateMemoryArtifactRequest"
          experience: "#/components/schemas/CreateExperienceArtifactRequest"
          skill: "#/components/schemas/CreateSkillArtifactRequest"
          handoff: "#/components/schemas/CreateHandoffArtifactRequest"
    CreateMemoryArtifactRequest:
      type: object
      additionalProperties: false
      required: [family, content]
      properties:
        family:
          type: string
          enum: [memory]
        content:
          $ref: "#/components/schemas/CreateMemoryArtifactContent"
    CreateExperienceArtifactRequest:
      type: object
      additionalProperties: false
      required: [family, content]
      properties:
        family:
          type: string
          enum: [experience]
        content:
          $ref: "#/components/schemas/ExperienceProposal"
    CreateSkillArtifactRequest:
      type: object
      additionalProperties: false
      required: [family, content]
      properties:
        family:
          type: string
          enum: [skill]
        content:
          $ref: "#/components/schemas/SkillProposal"
    CreateHandoffArtifactRequest:
      type: object
      additionalProperties: false
      required: [family, content]
      properties:
        family:
          type: string
          enum: [handoff]
        content:
          $ref: "#/components/schemas/HandoffContent"
    CreateMemoryArtifactContent:
      type: object
      additionalProperties: false
      required: [entries]
      properties:
        entries:
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: "#/components/schemas/CreateMemoryArtifactEntry"
    CreateMemoryArtifactEntry:
      type: object
      additionalProperties: false
      required: [kind, text]
      properties:
        kind:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
          description: >-
            Open application-defined Memory kind. Recommended values are fact, preference, decision, constraint,
            and working_note. The Server validates and preserves the supplied value; it never guesses or replaces it.
        text:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
          description: Durable non-empty Memory entry text used to create the Entry Version and search projection.
    CreateSourceRequest:
      type: object
      additionalProperties: false
      required: [content]
      properties:
        source_type:
          type: string
          enum: [content]
          default: content
        content:
          description: JSON value persisted by the built-in content Source adapter.
    ListArtifactsRequest:
      type: object
      additionalProperties: false
      properties:
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 50
        cursor:
          type: string
          minLength: 1
          maxLength: 4096
          nullable: true
    ReplaceArtifactRequest:
      oneOf:
        - $ref: "#/components/schemas/ReplaceMemoryArtifactRequest"
        - $ref: "#/components/schemas/ReplaceExperienceArtifactRequest"
        - $ref: "#/components/schemas/ReplaceSkillArtifactRequest"
        - $ref: "#/components/schemas/ReplaceHandoffArtifactRequest"
    ReplaceMemoryArtifactRequest:
      type: object
      additionalProperties: false
      required: [content]
      properties:
        content:
          $ref: "#/components/schemas/ReplaceMemoryArtifactContent"
    ReplaceMemoryArtifactContent:
      type: object
      additionalProperties: false
      required: [entries]
      properties:
        entries:
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: "#/components/schemas/ReplaceMemoryArtifactEntry"
    ReplaceMemoryArtifactEntry:
      type: object
      additionalProperties: false
      required: [kind, text]
      properties:
        entry_id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[\x21-\x7E]+$'
          nullable: true
          description: Existing logical entry to revise. Omit to append a new entry.
        kind:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '.*\S.*'
          description: Open application-defined kind; the Server preserves the supplied value.
        text:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: '.*\S.*'
    ReplaceExperienceArtifactRequest:
      type: object
      additionalProperties: false
      required: [content]
      properties:
        content:
          $ref: "#/components/schemas/ExperienceProposal"
    ReplaceSkillArtifactRequest:
      type: object
      additionalProperties: false
      required: [content]
      properties:
        content:
          $ref: "#/components/schemas/SkillProposal"
    ReplaceHandoffArtifactRequest:
      type: object
      additionalProperties: false
      required: [content]
      properties:
        content:
          $ref: "#/components/schemas/HandoffContent"
    SourceRecord:
      type: object
      required: [scope_id, source_type, source_id, content, position, content_digest]
      properties:
        receipt_identity:
          $ref: "#/components/schemas/HandoffReceiptIdentity"
          nullable: true
          description: Server-owned identity attestation when this Source contains an enforced-mode Handoff Receipt.
        scope_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '.*\S.*'
        source_type:
          type: string
          enum: [content]
        source_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '^[\x21-\x7E]+$'
        content:
          description: Persisted canonical JSON content.
        position:
          type: integer
          minimum: 1
        content_digest:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
    SourceTypeReference:
      type: object
      additionalProperties: false
      required: [source_type, source_id]
      properties:
        source_type:
          type: string
          enum: [content]
        source_id:
          type: string
          minLength: 1
          maxLength: 256
          pattern: '^[\x21-\x7E]+$'
    SourceReference:
      type: object
      additionalProperties: false
      required: [name, source_id]
      properties:
        name:
          type: string
          description: Stable Source type.
        source_id:
          type: string
    CaptureStatus:
      type: string
      enum: [accepted]
    BaseArtifactFamily:
      type: string
      enum: [memory, experience, skill, handoff]
    StatsPeriod:
      type: string
      enum: [today, 7d, 30d]
    CandidateFamily:
      type: string
      enum: [experience, skill]
    ExternalSkillInstallationScope:
      type: string
      enum: [user, project, plugin]
    ExternalSkillResolutionStatus:
      type: string
      enum: [available, unavailable]
    CandidateStatus:
      type: string
      enum: [pending, approved, rejected]
    PreparedContextSchema:
      type: string
      enum: [powercontext.prepared-context.v1]
    PreparedContextStatus:
      type: string
      enum: [ready, empty]
    EntryChangeOperation:
      type: string
      enum: [add, revise, deactivate, reactivate]
    FlushStatus:
      type: string
      enum: [idle, processed]
    MemoryEntryState:
      type: string
      enum: [active, inactive]
    MemoryMatchedBy:
      type: string
      enum: [fts, vector]
    MemorySearchMode:
      type: string
      enum: [auto, fts, vector, hybrid]
    MemoryUsedSearchMode:
      type: string
      enum: [fts, vector, hybrid]
    HandoffClaim:
      type: string
      enum: [state, next_action]
    HandoffActivationStatus:
      type: string
      enum: [generated, ignored]
    HandoffDisposition:
      type: string
      enum: [continuable, blocked, complete]
    HandoffEvidenceStatus:
      type: string
      enum: [available, unavailable]
    HandoffResolutionStatus:
      type: string
      enum: [empty, resolved]
    HandoffSchema:
      type: string
      enum: [powercontext.handoff.v1]
    HandoffSelection:
      type: string
      enum: [prepared, exact, latest]
    PreparedHandoffSchema:
      type: string
      enum: [powercontext.prepared-handoff.v1]
    AccessPrincipal:
      type: object
      additionalProperties: false
      required: [type, id]
      properties:
        type: {type: string, enum: [user, service]}
        id: {type: string, minLength: 1, maxLength: 255}
        description: {type: string, minLength: 1, maxLength: 255, nullable: true}
    AccessGroup:
      type: object
      additionalProperties: false
      required: [type, id]
      properties:
        type: {type: string, enum: [group]}
        id: {type: string, minLength: 1, maxLength: 255}
        description: {type: string, minLength: 1, maxLength: 255, nullable: true}
    AccessSubject:
      oneOf:
        - $ref: "#/components/schemas/AccessPrincipal"
        - $ref: "#/components/schemas/AccessGroup"
      discriminator:
        propertyName: type
        mapping:
          user: "#/components/schemas/AccessPrincipal"
          service: "#/components/schemas/AccessPrincipal"
          group: "#/components/schemas/AccessGroup"
    AccessControlMode:
      type: string
      enum: [disabled, enforced]
    AccessProviderCapabilities:
      type: object
      additionalProperties: false
      required:
        - safe_resource_filtering
        - multi_requirement_check
        - relationship_management
        - group_subjects
        - multi_principal
        - max_direct_resource_keys
      properties:
        safe_resource_filtering: {type: boolean}
        multi_requirement_check: {type: boolean}
        relationship_management: {type: boolean}
        group_subjects: {type: boolean}
        multi_principal: {type: boolean}
        max_direct_resource_keys: {type: integer, minimum: 1, maximum: 10000}
    ArtifactFamilyAccessCapability:
      type: object
      additionalProperties: false
      required: [family, enabled, share_unit, actions, grantable_roles]
      properties:
        family: {type: string, minLength: 1, maxLength: 128}
        enabled: {type: boolean}
        share_unit:
          type: string
          enum: [artifact, memory_entry]
        actions:
          type: array
          items:
            $ref: "#/components/schemas/AccessAction"
        grantable_roles:
          type: array
          items:
            $ref: "#/components/schemas/AccessRole"
    AccessMeResponse:
      type: object
      additionalProperties: false
      required:
        - principal
        - mode
        - resource_kinds
        - provider_capabilities
        - artifact_families
      properties:
        principal:
          $ref: "#/components/schemas/AccessPrincipal"
        mode:
          $ref: "#/components/schemas/AccessControlMode"
        resource_kinds:
          type: array
          items:
            $ref: "#/components/schemas/AccessResourceType"
        provider_capabilities:
          $ref: "#/components/schemas/AccessProviderCapabilities"
        artifact_families:
          type: array
          items:
            $ref: "#/components/schemas/ArtifactFamilyAccessCapability"
    AccessAction:
      type: string
      enum:
        - server.observe
        - server.admin
        - scope.read
        - scope.contribute
        - scope.review
        - scope.delegate
        - scope.admin
        - artifact.read
        - artifact.write
        - artifact.share
        - handoff.evidence.inspect
        - handoff.acknowledge
        - prompt.use
    AccessResourceType:
      type: string
      enum: [server, scope, artifact]
    ServerAccessResource:
      type: object
      additionalProperties: false
      required: [type, deployment_id]
      properties:
        type: {type: string, enum: [server]}
        deployment_id: {type: string, minLength: 1, maxLength: 128, pattern: '^[\x21-\x7E]+$'}
    ScopeAccessResource:
      type: object
      additionalProperties: false
      required: [type, scope_id]
      properties:
        type: {type: string, enum: [scope]}
        scope_id: {type: string, minLength: 1, maxLength: 256, pattern: '.*\S.*'}
    MemoryEntryAccessSelector:
      type: object
      additionalProperties: false
      description: Logical Memory entry selector; the Binding covers the entry's existing and future versions.
      required: [type, entry_id]
      properties:
        type: {type: string, enum: [memory_entry]}
        entry_id: {type: string, minLength: 1, maxLength: 128, pattern: '^[\x21-\x7E]+$'}
    AccessArtifactIdentity:
      type: object
      additionalProperties: false
      description: Logical Artifact identity; the Binding covers existing and future Revisions of the same Artifact.
      required: [family, artifact_id]
      properties:
        family: {type: string, minLength: 1, maxLength: 128, pattern: '^[\x21-\x7E]+$'}
        artifact_id: {type: string, minLength: 1, maxLength: 128, pattern: '^[\x21-\x7E]+$'}
    ArtifactAccessResource:
      type: object
      additionalProperties: false
      required: [type, scope_id, identity]
      properties:
        type: {type: string, enum: [artifact]}
        scope_id: {type: string, minLength: 1, maxLength: 256, pattern: '.*\S.*'}
        identity:
          $ref: "#/components/schemas/AccessArtifactIdentity"
        selector:
          allOf:
            - $ref: "#/components/schemas/MemoryEntryAccessSelector"
          nullable: true
    AccessResource:
      oneOf:
        - $ref: "#/components/schemas/ServerAccessResource"
        - $ref: "#/components/schemas/ScopeAccessResource"
        - $ref: "#/components/schemas/ArtifactAccessResource"
      discriminator:
        propertyName: type
        mapping:
          server: "#/components/schemas/ServerAccessResource"
          scope: "#/components/schemas/ScopeAccessResource"
          artifact: "#/components/schemas/ArtifactAccessResource"
    AccessDecision:
      type: object
      additionalProperties: false
      required: [allowed, reason_code]
      properties:
        allowed: {type: boolean}
        reason_code: {type: string, minLength: 1, maxLength: 64}
    AccessRequirementMatch:
      type: string
      enum: [all, any]
    AccessCheckRequirement:
      type: object
      additionalProperties: false
      required: [action, resource]
      properties:
        action:
          $ref: "#/components/schemas/AccessAction"
        resource:
          $ref: "#/components/schemas/AccessResource"
    AccessCheckRequest:
      type: object
      additionalProperties: false
      required: [match, requirements]
      properties:
        match:
          $ref: "#/components/schemas/AccessRequirementMatch"
        requirements:
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: "#/components/schemas/AccessCheckRequirement"
    AccessCheckResponse:
      type: object
      additionalProperties: false
      required: [allowed, decisions]
      properties:
        allowed: {type: boolean}
        decisions:
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: "#/components/schemas/AccessDecision"
    ListAccessResourcesRequest:
      type: object
      additionalProperties: false
      required: [action, resource_type]
      properties:
        action:
          $ref: "#/components/schemas/AccessAction"
        resource_type:
          $ref: "#/components/schemas/AccessResourceType"
        family: {type: string, minLength: 1, maxLength: 128, nullable: true}
        cursor: {type: string, nullable: true}
        limit: {type: integer, minimum: 1, maximum: 500, default: 100}
    AccessResourcePage:
      type: object
      additionalProperties: false
      required: [items, total, next_cursor]
      properties:
        items:
          type: array
          maxItems: 500
          items:
            $ref: "#/components/schemas/AccessResource"
        total: {type: integer, minimum: 0}
        next_cursor: {type: string, nullable: true}
    AccessRole:
      type: string
      enum:
        - handoff.viewer
        - handoff.receiver
        - artifact.viewer
        - prompt.user
        - artifact.owner
        - scope.viewer
        - scope.contributor
        - scope.reviewer
        - scope.delegator
        - scope.admin
        - server.observer
        - server.admin
    AccessRoleCardinality:
      type: string
      enum: [many_per_resource, one_per_resource]
    ListAccessRolesRequest:
      type: object
      additionalProperties: false
      properties:
        resource_type:
          allOf:
            - $ref: "#/components/schemas/AccessResourceType"
          nullable: true
        family: {type: string, minLength: 1, maxLength: 128, nullable: true}
    AccessRoleDescriptor:
      type: object
      additionalProperties: false
      required:
        - role
        - resource_type
        - cardinality
        - actions
        - artifact_families
        - assignable_subject_types
        - system_managed
      properties:
        role:
          $ref: "#/components/schemas/AccessRole"
        resource_type:
          $ref: "#/components/schemas/AccessResourceType"
        cardinality:
          $ref: "#/components/schemas/AccessRoleCardinality"
        actions:
          type: array
          items:
            $ref: "#/components/schemas/AccessAction"
        artifact_families:
          type: array
          items: {type: string, minLength: 1, maxLength: 128}
        assignable_subject_types:
          type: array
          items: {type: string, enum: [user, service, group]}
        system_managed: {type: boolean}
    AccessRolePage:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 16
          items:
            $ref: "#/components/schemas/AccessRoleDescriptor"
    AccessBindingState:
      type: string
      enum: [active, revoked]
    AccessBinding:
      type: object
      additionalProperties: false
      required:
        - binding_id
        - subject
        - resource
        - role
        - granted_by
        - reason
        - created_at
        - expires_at
        - state
        - version
        - policy_revision
        - idempotency_key
        - revoked_at
        - revoked_by
      properties:
        binding_id: {type: string, minLength: 1, maxLength: 64}
        subject:
          $ref: "#/components/schemas/AccessSubject"
        resource:
          $ref: "#/components/schemas/AccessResource"
        role:
          $ref: "#/components/schemas/AccessRole"
        granted_by:
          $ref: "#/components/schemas/AccessPrincipal"
        reason: {type: string, maxLength: 1024, nullable: true}
        created_at: {type: string, format: date-time}
        expires_at: {type: string, format: date-time, nullable: true}
        state:
          $ref: "#/components/schemas/AccessBindingState"
        version: {type: integer, minimum: 1}
        policy_revision: {type: string, minLength: 1, maxLength: 64}
        idempotency_key: {type: string, minLength: 1, maxLength: 255}
        revoked_at: {type: string, format: date-time, nullable: true}
        revoked_by:
          allOf:
            - $ref: "#/components/schemas/AccessPrincipal"
          nullable: true
    ListAccessBindingsRequest:
      type: object
      additionalProperties: false
      required: [management_resource]
      properties:
        management_resource:
          $ref: "#/components/schemas/AccessResource"
        subject:
          allOf:
            - $ref: "#/components/schemas/AccessSubject"
          nullable: true
        role:
          allOf:
            - $ref: "#/components/schemas/AccessRole"
          nullable: true
        state:
          allOf:
            - $ref: "#/components/schemas/AccessBindingState"
          nullable: true
        cursor: {type: string, maxLength: 2048, nullable: true}
        limit: {type: integer, minimum: 1, maximum: 500, default: 100}
    AccessBindingPage:
      type: object
      additionalProperties: false
      required: [items, next_cursor]
      properties:
        items:
          type: array
          maxItems: 500
          items:
            $ref: "#/components/schemas/AccessBinding"
        next_cursor: {type: string, maxLength: 2048, nullable: true}
    CreateAccessBindingRequest:
      type: object
      additionalProperties: false
      required: [subject, resource, role, idempotency_key]
      properties:
        subject:
          $ref: "#/components/schemas/AccessSubject"
        resource:
          $ref: "#/components/schemas/AccessResource"
        role:
          $ref: "#/components/schemas/AccessRole"
        idempotency_key: {type: string, minLength: 1, maxLength: 255}
        reason: {type: string, maxLength: 1024, nullable: true}
        expires_at: {type: string, format: date-time, nullable: true}
    RevokeAccessBindingRequest:
      type: object
      additionalProperties: false
      required: [binding_id, expected_version, idempotency_key]
      properties:
        binding_id: {type: string, minLength: 1, maxLength: 64}
        expected_version: {type: integer, minimum: 1}
        idempotency_key: {type: string, minLength: 1, maxLength: 255}
    AccessBindingReplacementInput:
      type: object
      additionalProperties: false
      required: [subject]
      properties:
        subject:
          $ref: "#/components/schemas/AccessSubject"
        reason: {type: string, maxLength: 1024, nullable: true}
        expires_at: {type: string, format: date-time, nullable: true}
    ReplaceAccessBindingRequest:
      type: object
      additionalProperties: false
      required: [binding_id, expected_version, replacement, idempotency_key]
      properties:
        binding_id: {type: string, minLength: 1, maxLength: 64}
        expected_version: {type: integer, minimum: 1}
        replacement:
          $ref: "#/components/schemas/AccessBindingReplacementInput"
        idempotency_key: {type: string, minLength: 1, maxLength: 255}
    AccessBindingReplacement:
      type: object
      additionalProperties: false
      required: [previous, current]
      properties:
        previous:
          $ref: "#/components/schemas/AccessBinding"
        current:
          $ref: "#/components/schemas/AccessBinding"
    ListAccessAuditRequest:
      type: object
      additionalProperties: false
      required: [resource]
      properties:
        resource:
          oneOf:
            - $ref: "#/components/schemas/ServerAccessResource"
            - $ref: "#/components/schemas/ScopeAccessResource"
          discriminator:
            propertyName: type
        action:
          allOf:
            - $ref: "#/components/schemas/AccessAction"
          nullable: true
        subject:
          allOf:
            - $ref: "#/components/schemas/AccessSubject"
          nullable: true
        result: {type: string, enum: [allowed, denied], nullable: true}
        time_range:
          allOf:
            - $ref: "#/components/schemas/AccessAuditTimeRange"
          nullable: true
        cursor: {type: string, maxLength: 2048, nullable: true}
        limit: {type: integer, minimum: 1, maximum: 500, default: 100}
    AccessAuditTimeRange:
      type: object
      additionalProperties: false
      required: [start, end]
      properties:
        start: {type: string, format: date-time}
        end: {type: string, format: date-time}
    AccessAuditEvent:
      type: object
      additionalProperties: false
      required:
        - cursor
        - event_id
        - occurred_at
        - request_id
        - transport
        - operation
        - principal
        - actor
        - action
        - resource
        - allowed
        - reason_code
        - policy_revision
        - matched_subject
        - binding_id
        - target
        - role
        - expected_version
        - result_version
      properties:
        cursor: {type: integer, minimum: 1}
        event_id: {type: string, minLength: 1, maxLength: 64}
        occurred_at: {type: string, format: date-time}
        request_id: {type: string, maxLength: 128, nullable: true}
        transport: {type: string, minLength: 1, maxLength: 16}
        operation: {type: string, minLength: 1, maxLength: 128}
        principal:
          $ref: "#/components/schemas/AccessPrincipal"
        actor:
          allOf:
            - $ref: "#/components/schemas/AccessPrincipal"
          nullable: true
        action:
          $ref: "#/components/schemas/AccessAction"
        resource:
          $ref: "#/components/schemas/AccessResource"
        allowed: {type: boolean}
        reason_code: {type: string, minLength: 1, maxLength: 64}
        policy_revision: {type: string, minLength: 1, maxLength: 64, nullable: true}
        matched_subject:
          allOf:
            - $ref: "#/components/schemas/AccessSubject"
          nullable: true
        binding_id: {type: string, maxLength: 64, nullable: true}
        target:
          allOf:
            - $ref: "#/components/schemas/AccessSubject"
          nullable: true
        role:
          allOf:
            - $ref: "#/components/schemas/AccessRole"
          nullable: true
        expected_version: {type: integer, minimum: 1, nullable: true}
        result_version: {type: integer, minimum: 1, nullable: true}
    AccessAuditPage:
      type: object
      additionalProperties: false
      required: [items, next_cursor]
      properties:
        items:
          type: array
          maxItems: 500
          items:
            $ref: "#/components/schemas/AccessAuditEvent"
        next_cursor: {type: string, maxLength: 2048, nullable: true}
